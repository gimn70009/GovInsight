# GovInsight 공통 작업 지침

## 적용 범위

- 이 문서는 저장소 전체에 적용한다.
- 하위 폴더의 `AGENTS.md`에는 해당 영역의 추가 규칙을 둔다.
- 하위 규칙이 공통 규칙과 충돌하면 해당 폴더에서는 하위 규칙을 우선한다.

## 프로젝트 문서

- 구현 전에 `docs/DESIGN.md`에서 관련 설계를 확인한다.
- 작업 시작 전에 `docs/PLAN.md`에서 현재 단계와 다음 작업을 확인한다.
- 설계 또는 기능 동작이 변경되면 `docs/DESIGN.md`를 함께 수정한다.
- 계획된 작업을 완료하면 `docs/PLAN.md`의 진행 상태를 갱신한다.
- 커밋 메시지는 `CONTRIBUTING.md`의 규칙을 따른다.

## 공통 원칙

- 비밀번호, API 키, 토큰 및 개인정보를 Git에 커밋하지 않는다.
- 사용자가 요청하지 않은 영역을 함께 수정하지 않는다.
- 아직 결정되지 않은 기술이나 기능을 임의로 확정하지 않는다.
- 테스트하지 못한 항목은 완료된 것처럼 보고하지 않는다.
