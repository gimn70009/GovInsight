# 설계 요약

GovInsight는 공고를 수집한 뒤 회사와의 관련성, 참여 방향, 신청서 초안까지 검토할 수 있도록 연결합니다. 이 문서는 구조와 핵심 규칙을 설명합니다.

[프로젝트 소개](../README.md) · [주요 구현 내용](IMPLEMENTATION.md)

## 역할을 나눈 이유

| 구성 | 맡은 일 | 선택 이유 |
| --- | --- | --- |
| React · TypeScript | 모니터링 관리, 분석·비교·초안 화면 | 긴 분석 결과를 단계별로 보여주고 생성 상태를 관리 |
| Spring Boot · Oracle | 인증, 실행 상태, 문서 버전, 결과 저장·조회 | 데이터와 트랜잭션의 책임을 한곳에 집중 |
| FastAPI · Playwright | 게시판 수집, 첨부파일 읽기, 작업 접수 | 기관별 페이지와 문서 형식을 독립적으로 처리 |
| LangChain · LangGraph | 근거 선택, 회사 관점 분석, 제안·초안 생성 | 모델 판단과 실행 순서·검증을 분리 |

Python은 Oracle에 직접 접근하지 않고 HTTP로 결과를 전달합니다. 모델을 기다리는 동안 DB 트랜잭션을 유지하지 않습니다.

## 한 번의 모니터링이 끝나기까지

```text
실행 요청 → 수집 → 변경 감지·저장 → 공고 분석 → 사업 제안 후속 처리 → 보고서 저장 → 완료
```

- 작업 요청에는 먼저 접수 상태와 작업 ID를 반환하고, 결과는 후속 HTTP 요청으로 전달합니다.
- `COLLECTED`는 수집 결과를 저장한 상태입니다. 사업 제안 후속 처리와 보고서 저장까지 끝나면 `COMPLETED`가 됩니다.
- 사업 제안 대상이 없어도 후속 처리 완료를 전달해 보고서 단계로 넘어갑니다.
- 사용자가 양식을 선택하는 **신청서 초안 생성은 별도 요청**이며 모니터링 완료 조건에 포함하지 않습니다.
- 자동 실행은 한국 시간 기준 일정으로 같은 파이프라인을 사용합니다. 진행 중인 실행이 있으면 중복 실행을 건너뜁니다.
- 자동 모니터링 사용 토글은 클릭 즉시 기존 일정 저장 API로 사용 여부를 저장하고, 서버가 성공 응답한 상태를 표시합니다. 실패하면 기존 상태와 오류 안내를 유지합니다. 토글은 마지막으로 저장한 주기·요일·시각을 유지하며, 편집 중인 일정은 별도 `일정 저장`으로 반영합니다. 조회 전과 저장 중에는 설정 변경을 막고, 저장과 겹친 과거 조회 응답이 토글 상태를 덮어쓰지 않게 합니다.

## 같은 공고를 다시 발견했을 때

문서, 문서 버전, 실행별 감지 이력을 구분합니다.

| 저장 단위 | 목적 |
| --- | --- |
| 문서·버전·첨부파일 | 현재 내용과 과거 변경 내용을 함께 보관 |
| 실행별 감지 이력 | 언제 어떤 공고를 발견했는지 기록 |
| 분석·제안·초안 | 결과가 어느 문서 버전에 근거했는지 연결 |

제목·본문·첨부 정보와 파일 내용의 SHA-256을 비교합니다. 내용이 같으면 버전을 늘리지 않고, 해당 버전의 유효한 분석을 재사용합니다. 파일명이 같아도 내용이 바뀌면 감지하며, 분석 누락·실패·구버전 결과는 재처리 대상으로 구분합니다.

### 수정 공고의 설명

수정 공고는 직전 버전의 제목·본문과 첨부별 추출 텍스트를 현재 버전과 비교합니다. `이 공고에서 중요하게 볼 점`은 **변경 전 → 변경 후 → 회사에 미치는 영향** 순서로 중요한 조건 변경을 먼저 설명합니다. 첫 번째 항목은 현재 회사와의 접점을 유지하고, 두 번째 항목과 중복하지 않습니다. 접수 종료 안내가 필요하면 그 다음에 변경 설명을 이어갑니다.

- 같은 이름의 첨부가 각 버전에 하나씩 있을 때 본문을 비교합니다. 추가·삭제, 읽기 실패, 중복 이름은 따로 표시합니다. 이름이 달라진 파일을 임의로 같은 파일의 개정본으로 연결하지 않습니다.
- ZIP은 기존에 추출·저장한 내부 문서의 결합 텍스트를 비교합니다. 내부 파일별 원본 구조·이미지까지 비교하는 기능은 아닙니다.
- 이전 첨부 목록이 미전달된 경우와 실제 빈 목록을 구분합니다. 이전 자료 누락·파싱 실패·길이 제한이 있으면 확인된 변경만 설명하고 비교 한계를 밝힙니다. 비교 불가를 변경 없음으로 해석하지 않습니다.
- 본문과 첨부별 비교 입력은 각 200,000자, 첨부 이름은 최대 20개로 제한하며 변경 가능성이 있는 파일을 먼저 처리합니다. 차이 텍스트의 합계는 `ANALYSIS_MAX_TEXT_CHARS` 이내입니다. 입력·차이 생략 여부와 누락 파일 수를 함께 전달합니다. 차이의 줄 번호는 `excerptStartLine`부터 시작한 발췌문 기준입니다.
- 원문 차이 계산에는 모델 호출을 추가하지 않습니다. 의미와 회사 영향은 기존 분석 모델이 판단합니다. 이미 저장된 분석은 자동 교체하지 않으며 이후 새로 분석되는 수정 공고부터 적용됩니다.

## 첨부파일과 AI 결과를 다루는 기준

- PDF·HWP·HWPX와 ZIP 내부 문서를 읽습니다. 확장자뿐 아니라 실제 파일 구조도 확인합니다.
- 파일 크기·개수·압축 해제 용량·처리 시간을 제한하고, 읽지 못한 첨부는 사유를 남깁니다.
- ZIP에서 추출 본문이 같은 파일은 묶되 내부 문서 순번을 보존해 저장 초안의 연결이 바뀌지 않게 합니다.
- AI는 제공된 원문과 회사 정보를 바탕으로 의미를 판단합니다. 회사의 확인된 사실과 데모 가정은 입력에서 구분합니다.
- 코드는 출력 스키마, 원문 인용, 작성 언어와 형식을 검증합니다. 호출·재시도·전체 실행 시간에는 상한을 둡니다.
- 함께 신청할 때의 주의사항은 실제 조항을 근거로 정리합니다. 확인되지 않은 자격·제한을 사실처럼 채우지 않습니다.

### 공고 분석의 근거 조사 에이전트

- 공고 분석 앞단에서 LangChain `create_agent`의 LangGraph 실행 루프를 사용합니다. 모델이 제공된 조회 도구와 순서를 선택하고, 실행 결과를 받아 다음 조회 또는 종료를 결정합니다. 기존의 메타데이터 기반 분석 계획 호출은 이 근거 조사 단계로 대체합니다.
- 도구는 해당 문서의 공고 본문, 회사 정보, 추출된 첨부 텍스트, 이전 버전 비교, 이전 분석 조회로 제한합니다. 읽을 수 있는 첨부가 없거나 수정 공고가 아니면 관련 도구를 제공하지 않습니다. 수정 공고의 이전 버전이 없으면 비교 도구가 비교 불가를 명시합니다. 외부 검색·수집·파일 변경·DB 접근은 에이전트 도구에 포함하지 않습니다.
- 본문·회사 정보와 해당하는 첨부·수정 비교·이전 분석은 필수 확인 대상입니다. 모델이 순서를 결정하되, 실제 성공한 도구 응답과 실행 결과 캐시에 필수 자료가 모두 존재해야 최종 분석으로 진행합니다. 누락·조회 실패·한도 초과를 자동 자료 주입으로 숨기지 않고 기존의 제한된 재시도로 전달합니다.
- 조사 1회는 도구 요청 최대 8회, 모델 호출 최대 8회, 최대 45초로 제한합니다. 조사와 최종 분석·법률 검토를 합친 분석 시도 전체에도 `ANALYSIS_TIMEOUT_SECONDS`를 적용합니다. 전체 재시도는 `ANALYSIS_MAX_ATTEMPTS` 이내이며 각 시도는 새 조사 상태로 시작합니다. 시간 초과 후에는 기존 원문 길이 축소 정책을 적용합니다. 제한에 도달하면 실패할 수 있으며 추가 호출로 지연과 비용이 늘 수 있습니다.
- 최종 분석에는 실제 조회한 원문 도구 결과를 전달합니다. 조사 에이전트의 완료 문장·내부 추론은 최종 근거로 쓰거나 저장하지 않습니다. `usedTools`는 실제 성공한 조회의 이름과 순서를 기록하며, 반복 호출도 기록합니다. 자료 캐시와 대화 이력은 문서별·시도별로 격리합니다.
- 최종 구조화 출력·업무 규칙 검증·부분 결과 보존·사업 제안·저장 API는 유지합니다. 법률 검토만 수행하는 요청은 기존 경로를 사용합니다. 저장된 분석은 자동 재실행하지 않으며 새로 분석되는 공고부터 적용합니다.

### 접수 종료 표시와 설명 문체

- 상세 API의 `analysis.applicationDeadline`은 저장된 비교 요약의 마감일을 전달합니다. 값이 없는 기존 결과는 제안 준비안의 마감일, 긴급도 설명의 명시적인 `신청 마감일은 …` 형식 순으로 보완합니다. 일반 요약의 `접수기간 종료 전`·`신청 불가능` 같은 문구로 접수 종료를 판단하지 않습니다.
- 화면에서는 한국 시간 기준으로 마감 시각과 현재 시각을 비교합니다. 날짜만 있으면 해당 날짜가 끝날 때까지 유지하며, 해석할 수 없거나 모호한 날짜는 마감으로 단정하지 않습니다. 접수기간 전체에 연도가 명시되면 종료일을 사용합니다.
- AI의 긴급도 점수는 기존의 분석일 기준 날짜 규칙을 유지합니다. 날짜를 확인하면 긴급도 설명을 다시 구성하여 미래 마감일과 과거의 `마감 지남` 문구가 혼재하거나 남은 기간이 중복되지 않게 합니다. 지원 자격 판단 자체를 마감일만으로 지원 가능으로 바꾸지는 않습니다.
- `함께 신청할 때 주의할 점`의 생성 설명은 알려진 평서형·명사형 종결을 합니다체로 보정한 뒤 문장 완결성을 검사합니다. 안전하게 고치기 어려운 문장은 해당 유형의 기존 재시도 대상으로 삼고, 완료된 다른 유형은 보존합니다. 최종 보완 실패 시 검증된 원문 근거와 기본 안내를 유지합니다.
- 이미 저장된 설명도 화면에서 알려진 종결을 같은 방식으로 보정합니다. 관련 원문·인용은 변환하지 않으며 저장된 분석을 일괄 덮어쓰지 않습니다.

### 긴 원문의 근거 선택 (2026-09-22)

- 본문·첨부 분석 도구는 길이 초과 시 앞/뒤 고정 비율 절단 대신 절·문단 경계를 우선하고, 긴 절은 완전한 줄/문장 경계로 나눕니다. 자격·제외 조건, 기한·제출, 지원 규모, 사업 목적·역할, 문의를 나타내는 키워드의 범위를 우선 확보한 뒤 원문 순서로 전달합니다. 임베딩이나 의미 이해 모델을 이용하는 선택기가 아니며 모든 중요 조건의 보존을 보장하지 않습니다.
- 한 구간은 약 1,600자 단위로 묶고, 이어지는 `단,`·`다만`·`※` 조건은 앞 구간에 붙입니다. 제한보다 큰 분할 불가 문장은 중간을 잘라 확정적 조건으로 만들지 않고 생략합니다. 긴 입력에서 선택 구간은 최대 64개입니다. 짧은 원문은 그대로 전달합니다.
- 첨부별로 입력 분량을 균등 배분하고 짧은 파일의 남는 몫을 다른 파일에 재배분합니다. 추출 텍스트가 완전히 같은 파일은 SHA-256으로 묶어 한 번만 전달하고 다른 파일은 `duplicateOfAttachmentId`로 표시합니다. 같은 파일명만으로 중복 처리하지 않습니다.
- 도구 결과의 `coverage`는 입력 원문 길이, 선택된 원문 문자 수, 생략 여부, 원문 내 start 포함/end 제외 문자 위치를 제공합니다. JSON과 메타데이터는 원문 문자 한도 밖이며 본문과 첨부 합계의 예산은 각각 기존 `ANALYSIS_MAX_TEXT_CHARS`를 유지합니다. 전체 프롬프트 토큰 수 상한으로 해석하지 않습니다.
- 읽을 수 없는 파일은 메타데이터로 남깁니다. 본문과 첨부 모두 선택된 원문이 없으면 최종 분석을 진행하지 않습니다. 생략 구간을 '조건 없음'으로 간주하지 않고 대상·국가·차수·단계가 다른 기한/금액을 섞지 않도록 조사 및 분석 프롬프트에 안내합니다. 이는 모든 주장에 대한 자동 의미 검증을 뜻하지 않습니다.
- 근거 조사 에이전트의 도구 선택·순서 결정, 기존 구조화 출력, 모델 호출/재시도 상한을 유지합니다. 추가 모델·임베딩 호출이나 새로운 DB 필드는 없습니다. 기존 분석 재사용 정책은 유지되므로 이미 저장된 분석은 자동 갱신되지 않습니다.

### 공고 분석의 시간 초과 재시도

- Python·HTTPX·모델 SDK의 시간 초과 예외와 연결된 원인 예외를 종류로 판별하고 표준 메시지로 전달합니다. 오류 문구의 언어·내용에 의존하지 않으며 시간 초과 메시지에 원본 예외의 비공개 내용을 노출하지 않습니다.
- 시간 초과 후에는 원문·첨부·버전 비교에 적용하는 텍스트 한도를 `min(ANALYSIS_MAX_TEXT_CHARS, 24000)`으로 낮춥니다. 기본 40,000자에서 24,000자로 조정하며 전체 프롬프트 길이 제한을 뜻하지는 않습니다. 일반 검증 실패는 이 축소를 적용하지 않습니다.
- 같은 모델·응답 스키마·시도별 제한 시간을 유지하고, 재시도 전용 작성 지시를 추가합니다. 요약은 보통 300~600자, 두 공고 해석의 각 설명은 보통 400~700자를 권장하되 필수 정보가 많으면 기존 필드 상한까지 작성합니다. 별도의 낮은 출력 토큰 상한으로 응답을 강제 절단하지 않습니다.
- 신청 자격·제외 조건·파트너·기한과 시각·지원 금액과 단위·비율·자부담·제출 서류·평가 기준·변경 전후와 회사 영향·미확인 사항을 보존하도록 지시합니다. 줄이는 대상은 반복·일반론·부연이며 필수 필드·판단 근거·조건과 부정 표현은 유지합니다. 실제 의미 보존은 모델 판단이며 테스트로 전체 공고의 품질을 보장하지 않습니다.
- 일부 설명 오류만 제거한 뒤 근거·정책·스키마 검증을 통과한 결과가 있으면, 이후 재시도가 시간 초과되어도 그 부분 결과를 보존합니다. 해당 설명에는 생략 안내를 둡니다. 처음부터 모든 응답을 받지 못했거나 핵심 검증을 통과하지 못했다면 해당 게시글 분석은 실패할 수 있습니다. 응답을 항목별로 나눠 저장하거나 미완성 스트림에서 회수하는 구조는 아닙니다.
- 로그에는 `compact_retry`와 `source_text_limit`을 남겨 축소 적용을 확인합니다. 원문·모델 응답 본문은 이 로그에 포함하지 않습니다.

### 제안 전략의 기업 역량·활용 방향 표시

- 제안 전략의 접힘 영역은 `기업 역량·활용 방향 및 지원 중단 기준 보기`, 내부 제목은 `기업 역량과 활용 방향`으로 표시합니다. 회사의 보유 역량·실적과 이를 활용하는 방향을 구분합니다.
- 해당 영역의 항목명은 `보유 역량·실적`과 `활용 방향`을 사용합니다. 이번 변경은 화면 문구에 한정하며 기존 생성·저장 데이터와 API 필드는 유지합니다.

## 유사 공고를 고르는 방법

1. 제목·사업 목적·자격·파트너 정보를 정리하고, 미확인 안내 문구는 검색 입력에서 제외합니다.
2. 저장된 임베딩의 의미 점수와 제목·목적의 어휘 검색 순위를 결합합니다.
3. 주제 충돌과 공통 근거를 확인해 최대 3건을 반환합니다. 정보가 부족하면 선정을 보수적으로 제한합니다.

문서 특징과 통계를 캐시하고 상위 후보만 계산합니다. **조회할 때 추가 모델 호출은 없습니다.** 점수는 선정에 쓰는 유사도이며 같은 사업일 확률을 뜻하지 않습니다.

[오탐 개선과 속도 측정](evaluations/notice-similarity-2026-09-10.md)

## 초안 생성·재작성의 저장 규칙

- 사업·연구·협력 내용을 서술하는 작성란을 대상으로 합니다. 명확한 공고·동의서·증명서 등은 제외하고, 혼합 문서는 실제 작성란을 검토합니다.
- 선택한 문서 버전·첨부·ZIP 내부 문서를 검증하고, 양식 언어에 맞춰 핵심 항목을 최대 4개 작성합니다.
- 파일명 단어로 초안 대상을 확정하거나 제외하지 않습니다. 목록 API는 읽기 가능 여부만 전달하고, 화면은 별도 본문 판별 API로 실제 사업·연구·협력 작성란을 확인한 파일만 생성 가능하게 표시합니다. 판별은 전체 추출 본문과 원문 제목 후보를 모델에 전달하며, 선택한 항목의 원문 줄 번호·인용·중복을 검증합니다. FAQ 질문·답변, 공고 설명, 제출 목록, 이름·서명 등의 단순 입력란은 서술형 작성란과 구분하고 혼합 파일의 실제 양식은 허용합니다.
- 초안 본문 응답은 검증된 항목별로 `section_1`부터 필요한 번호까지 필수 필드를 지정한 구조화 스키마를 사용합니다. 모델이 항목 번호나 배열 개수를 다시 정하지 않으며, 코드는 필드 이름을 기존 `section_id`로 변환한 후 항목·분량·문체·근거를 재검증합니다. 문체·작성 안내 보완 시에는 실패 항목의 필드만 요청하고 기존 정상 항목과 근거·확인 사항을 보존합니다. 화면·저장 API의 목록 형식은 유지합니다.
- 본문 분량은 모델에 기존처럼 보통 500~900자를 권장합니다. 모델 응답의 본문을 먼저 파싱한 뒤 기존 400~2,600자 제한으로 검증하므로, 분량 오류가 있는 응답도 보완 입력으로 전달할 수 있습니다. 분량 보완 요청에는 해당 항목 번호·현재 글자 수·허용 범위를 함께 전달하고 모든 필수 항목을 반환하도록 지시합니다. 호출 횟수와 시간 한도는 늘리지 않으며 검증되지 않은 결과는 저장하지 않습니다.
- 한국어 초안은 명확한 문장 중간 줄바꿈·마침표 누락·전각 마침표만 먼저 보정합니다. 단어·수치·부정·조건은 고치지 않습니다. 명사형 종결·미완성 문장·소제목·불릿은 문체 보완 대상으로 탐지하며 이것만으로 초안 전체를 폐기하지 않습니다. 한국어 포함 여부·분량·항목·근거 ID·작성 안내문 금지 검증은 모든 항목에서 먼저 수행합니다.
- 작성 안내 검사는 독자 호칭의 단어·조사 경계, 직접 작성 지시와 문장 끝의 작성 의무 표현, 내부 회사 프로필 언급, 같은 대괄호 안의 확인용 빈칸을 구분합니다. `복귀하는`·`회귀하는`과 `작성해야 하는 보고서`를 안내문으로 오인하지 않으며, 문장 처음에 당사·우리 회사가 주어로 나오는 작성 의무 설명도 허용합니다. 작성란 지시 속 종속절에 회사가 언급되는 경우는 이 예외로 허용하지 않습니다. 이는 규칙 기반 검사로 모든 문장의 의미를 검증하는 기능은 아닙니다.
- 작성 안내 오류는 다른 모든 항목의 분량·근거 등 필수 검증을 확인한 뒤 보완합니다. 실패 항목·문장 번호·규칙 코드만 로그에 남기고, 문제 표현·문장·본문은 수정 대상 데이터로 모델에 전달합니다. 안내문 오류와 함께 존재하는 한국어 문체 오류 항목을 모아 해당 본문만 다시 작성하고, 정상 본문 및 모든 항목의 근거·확인 사항은 유지합니다. 이전 전체 입력·응답도 보완 문맥에 포함되므로 부분 보완이 입력 토큰 감소를 뜻하지는 않습니다.
- 한국어 문장 구분에서는 닫힌 괄호·인용부호(일반 작은따옴표·큰따옴표 포함) 내부와 URL의 마침표를 문장 끝으로 오인하지 않습니다. 영문 소유격·이름의 아포스트로피는 따옴표 경계로 취급하지 않으며 닫히지 않은 인용부호는 보호하지 않습니다. 미완성 문장·실제 명사형 종결 탐지는 문체 보완 판단에 계속 사용합니다.
- 한국어 문체 오류는 본문을 로그에 남기지 않고 항목·보정 후 문장 번호·오류 유형으로 진단합니다. 재시도 입력에는 모든 문체 오류 항목의 본문과 각 항목의 첫 실패 문장을 수정 대상 데이터로 전달합니다. 문제가 있는 항목만 반환하도록 요청하며 필요한 내용·수치·조건·부정·계획과 사실의 구분을 유지합니다.
- 문체·작성 안내 재시도는 대상 항목의 본문만 교체합니다. 정상 항목의 본문 및 모든 항목의 회사 근거 ID·확인 사항은 기존 값을 보존하고, 병합 결과를 기존 분량·문체·항목·근거 규칙으로 다시 검증합니다. 대상 누락·중복은 거부합니다. 문체 보완 전 모든 항목의 필수 검증을 통과한 원본이 있으면, 보완 응답의 분량 부족·문체 오류·모델 요청 실패 때 원본을 `COMPLETED`로 반환하고 제출 전 문장 표현을 다듬으라는 안내를 기존 `message`에 포함합니다. 실패한 보완 본문은 원본에 덮어쓰지 않습니다. 원본부터 필수 검증을 통과하지 못하면 이 보존 경로를 적용하지 않습니다. 영문 검증 정책은 유지합니다. 항목 선택 최대 2회와 본문 최대 2회를 별도로 제한하고 합계 최대 4회로 처리합니다. 항목 선정 재시도가 본문 보완 기회를 소진하지 않으며 전체 실행은 대기 시간을 포함해 최대 180초(설정값이 더 작으면 해당 값)로 제한합니다. 시간 초과·검증 실패·기타 실패는 안내를 구분하며, 로그의 `mode=style_repair` 또는 `mode=guidance_repair`와 `section_ids`로 보완 대상 수·번호를 확인할 수 있습니다. 작성 안내 오류는 필수 검증 실패이므로 해결되지 않은 원본을 완료 응답으로 반환하지 않습니다.
- 완료된 초안은 계정과 문서 버전별로 저장합니다. 재열람에는 모델을 호출하지 않습니다.
- 저장된 초안이 없는 파일은 목록 판별과 최초 생성에서 같은 양식 판별 결과를 재사용합니다. 저장된 초안이 있는 파일은 목록의 AI 판별에서 제외하고 저장본을 즉시 표시합니다. 재작성은 DB에 저장된 항목 제목·원문 인용·선정 사유를 전달하여 원문 일치와 중복만 코드로 검증한 뒤 같은 항목의 본문을 작성하며, 양식 여부를 AI로 다시 판별하지 않습니다. 저장 항목이 없거나 원문과 맞지 않으면 기존 초안을 보존하고 오류를 반환합니다. 캐시 키에는 파일명·본문·모델명·판별 지침이 포함되며 정상 판별만 1시간, 최대 128개를 프로세스 메모리에 보관합니다. 파일명은 모델의 보조 정보일 뿐 차단 규칙이 아닙니다. 같은 입력의 진행 중 판별도 합치며 판별은 최대 2회 호출, 대기 포함 90초, 동시 실행 2개·대기 포함 8건 이내입니다. 실패 결과는 캐시하지 않습니다. 본문은 수정 요청과 이전 본문을 받아 새로 생성하고 완료 캐시를 우회합니다. 새 결과가 검증·저장된 경우에만 교체하며 실패 시 기존 초안을 유지합니다.
- 제안서 초안 화면은 기존 시연 화면의 배치와 스타일을 유지합니다. 양식 카드에는 파일명·ZIP 이름·작성 완료 표시만 노출하고 판별한 항목 제목은 추가하지 않습니다. 같은 본문 파일과 사용할 수 없는 파일은 기존 접힘 목록을 유지합니다. 양식 판별은 사업 제안 탭 활성화 후 첨부 목록을 불러오면 스크롤 위치와 관계없이 시작합니다. 확인 중에는 “첨부 양식을 확인하고 있습니다.” 한 줄 안내를 표시하고, 확인된 양식부터 선택할 수 있습니다. 판별 실패는 별도의 접힌 “확인하지 못한 파일” 목록에서 다시 확인할 수 있게 하며, 저장된 초안은 파일명 규칙으로 숨기지 않고 계속 조회·복원할 수 있습니다. 저장된 초안의 재작성 가능 여부는 원문 읽기 가능 여부로 정하며 판별 캐시의 만료·비대상·실패에 영향을 받지 않습니다. 이전 초안 복원은 원문 읽기나 양식 판별과도 무관하게 저장된 이전 버전 유무로만 표시합니다.
- API 계약: 기존 GET /api/document-detections/{detectionId}/proposal-sources의 available은 파싱·길이 기준입니다. POST /proposal-sources/inspect에 attachmentId·partIndex를 전달하면 status(WRITABLE / NOT_WRITABLE / UNAVAILABLE)·sectionTitles·message를 반환합니다. 백엔드는 해당 문서 버전의 본문을 읽고 트랜잭션을 종료한 뒤 Python POST /internal/monitoring/proposal-template에 fileName·templateText를 전달합니다. 판별 실패를 NOT_WRITABLE로 바꾸지 않으며 신규 DB 테이블은 추가하지 않습니다. 재작성용 Python 요청의 previousSections에는 DB에서 읽은 title·body·sourceQuote·selectionReason을 전달합니다. 브라우저가 저장 항목을 지정하거나 변경하지 않습니다.
- 요청 ID로 재전송을 구분하고 저장 버전으로 충돌을 막습니다. 직전 초안 복원에는 모델 호출이 없습니다.
- 생성 진행 상태는 Spring 프로세스 메모리, 완료 결과는 DB에 보관합니다. 서버 재시작 시 미완료 작업이 자동 복구되는 구조는 아닙니다.

[영문 검증 개선](case-studies/english-draft.md) · [재작성과 실패 복구](case-studies/draft-regeneration.md)

## 운영에서 지키는 경계

JWT 인증과 계정별 조회 범위를 적용합니다. 비밀 설정은 환경변수나 Git에서 제외된 파일에 두고 원문 전체·개인정보·모델 내부 추론을 로그에 남기지 않습니다.

보고서 저장 후 설정된 Telegram·이메일 수신자에게 전송하고, 채널별·수신자별 발송 결과를 따로 기록합니다.

### 보고서 본문

- 보고서 생성은 경량 모델로 제출 안내를 정리한 뒤 고정 템플릿으로 조합합니다. 요약·기회점수는 저장 분석을 사용하고 모델로 재작성하지 않습니다. 발송·재전송과 HTML 렌더링에는 모델 호출이 없습니다. 기회점수 내림차순으로 정리합니다.
- 각 항목은 제목, 기관, 실제 문서 유형(일반 공지/사업 공고/제안 요청), 신규·수정·변경 없음 상태, 기회점수, 짧은 요약(최대 두 문장), 제출 주체·대상, 제출·의견 기한, 제출처·방법, 제출 서류, 문의 담당, 첨부파일 링크, 원문 링크를 표시합니다. 분석의 긴 참여 의견은 반복하지 않습니다.
- 보고서 요청 DTO는 저장 버전의 본문(최대 80,000자), 비교 요약, 첨부파일명·원본 다운로드 URL 및 파싱 완료 첨부 텍스트(파일당 40,000자, 합계 120,000자)를 전달합니다. 보고서에서는 확인된 제출서류에 연결되는 양식만 표시합니다. 전체 첨부 목록은 기존 게시글 상세에서 확인합니다. DB 스키마와 기존 분석 결과는 변경하지 않습니다.
- 제출 안내는 원문·첨부에서 경량 모델이 대상·기한·제출 방법·문의 정보를 별도 필드로 발췌하고 원문 일치를 검사합니다. 모델 실패/비활성화 시 표제 기반 추출과 기존 비교 분석으로 대체합니다. 모델 경로의 저장 신청 체크리스트는 추출 후보이며 원문에서 서류명·조건을 다시 확인합니다. 사업 제안 유무와 관계없이 추출하며 근거가 없는 저장 항목은 복사하지 않습니다. 문의 주소를 제출처로, 첨부파일 전체를 필수 서류로, 공개기간을 접수 마감으로 간주하지 않습니다. 여러 국가/경로의 안내는 함께 보존합니다. 추출하지 못한 정보는 `미확인 항목` 한 줄에 모아 표시하며, 사내 담당자를 임의로 배정하지 않습니다. 복잡한 표·긴 문맥·생략한 원문 범위는 자동 추출되지 않을 수 있습니다.
- 다운로드 URL은 수집된 HTTP(S) 주소만 사용하고 파일명을 링크로 표시합니다. 링크의 원래 쿼리 값을 보존하며 파일을 재업로드하거나 직접 발송하지 않습니다. 제공 사이트의 로그인·세션 요구나 링크 만료는 우회하지 않습니다.
- 메일 HTML은 저장된 본문을 게시글별 카드로 렌더링합니다. 제목은 22px 굵은 글씨, 기관·문서 유형·변경 상태·기회점수는 헤더, 요약과 제출 안내는 별도 영역, 첨부 링크는 파일별 행으로 표시합니다. 모바일을 고려해 인라인 스타일·표 기반 레이아웃을 사용합니다. 텔레그램은 제목·필드명·첨부 영역명을 굵게 표시합니다. 렌더링 단계에는 모델 호출이 없습니다.
- 원문 글머리표(▷·▶ 등)를 제거하고 표에서 줄바꿈된 표제와 값을 연결해 제출 안내 누락과 라벨 중복을 줄입니다. 다른 문의/제출 표제를 만나면 합치지 않습니다. 기존 분석의 문서 유형은 표시만 하며 이 단계에서 재분류하지 않습니다.
- 저장 본문은 제한된 `[파일명](URL)` 표기를 사용합니다. 텔레그램 HTML·이메일 HTML·상세 화면에서 안전한 링크로 렌더링하고 나머지 텍스트는 이스케이프합니다. 텔레그램은 링크 주소를 제외한 표시 문자 수를 기준으로 길이를 검사합니다. 일반 텍스트 이메일에도 URL을 보존합니다.
- 표시 본문은 UTF-16 기준 3,900자, 저장 본문은 20,000자 안으로 유지합니다. 긴 보고서는 안내 항목을 유지한 축약형으로 전환하고, 초과 첨부·게시글은 생략 건수와 원문/게시글 보기 안내를 표시합니다. URL 중간을 자르지 않습니다. 이미 저장된 보고서는 그대로이며 다음 보고서 생성부터 적용합니다.

- 보고서 제출 안내 추출은 표의 `역할`·`구분` 같은 머리글, 빈 칸, 신청기관 작성 예시를 값으로 사용하지 않습니다. 제출 안내의 표제 아래 여러 줄 예외 조건은 다음 표제/빈 줄 전까지 함께 읽고, 표시 한도를 넘으면 전체 조건을 원문에서 확인하도록 합니다. Windows 줄바꿈도 동일하게 처리합니다. 복잡한 표와 기존 분석의 사실 정확성을 보장하는 검증기는 아닙니다.
- 서류 다운로드 링크에는 긴 원본 파일명 대신 검증한 짧은 서류명을 사용합니다. 다운로드 주소의 쿼리는 보존합니다.

### 보고서 제출 준비 서류 (2026-09-22)

- 경량 모델이 활성화된 경우 저장 신청 체크리스트는 후보 목록으로만 전달합니다. 원문에서 확인한 신청 서류·조건을 사용하며 기존 목록의 오류를 그대로 복제하지 않습니다. 보고서와 저장 사업 제안 목록은 다를 수 있으며 생략 서류는 원문 확인으로 안내합니다.
- 모델 실패/비활성화의 기본 템플릿 경로는 기존 체크리스트를 재사용합니다. 실패 시 보고서에 자동 정리 실패 안내를 남깁니다. 원문 누락을 이유로 저장된 제안 데이터를 수정하지 않습니다.
- 구버전 `companyInputs`를 문서/조건/회의 안건으로 나누는 `PreparationCompatibility` 처리를 상세 화면과 보고서 요청이 공유합니다. 저장 JSON과 DB 스키마를 수정하지 않습니다.
- 항목에는 서류명과 확인된 다운로드 링크만 표시합니다. 자체 준비 증빙이나 양식을 특정하지 못한 항목은 이름만 표시합니다. 역할 추정·긴 설명·담당자 배정을 덧붙이지 않습니다. 제출 의무의 근거인 공고문과 실제 작성 양식을 구분합니다.
- 링크는 해당 버전에서 수집된 첨부 URL에 한정합니다. 이름의 구별 요소를 모두 대조하면서 단어 순서와 신청서/신청서식 표기를 정규화합니다. 설명 괄호는 매칭 시 제외하고 개인/단체 등의 식별 조건은 유지합니다. 양식 파일 내부의 독립된 제목과 작성 항목도 확인하며, 단순 제출서류 목록 언급만으로는 연결하지 않습니다. 복수 후보나 다른 종류의 신청서는 임의 선택하지 않습니다.
- ZIP 내부 양식은 `[파일: 이름]` 구간으로 확인하고 서류명에 `(ZIP)`을 붙여 원본 ZIP URL을 연결합니다. 내부 파일의 공개 URL을 만들거나 재호스팅하지 않습니다. 파싱/입력 생략으로 양식을 찾지 못할 수 있으며 다운로드 성공을 실시간 검증하지 않습니다.
- 신청 단계 체크리스트 유무와 관계없이 경량 모델이 원문에서 제출 서류명·조건·근거·양식 출처 ID를 추출합니다. 제출 의무가 확인되는 인용과 서류명/조건의 원문 일치를 검사하며, 단순 첨부 목록이나 선정 이후 서류를 신청 서류로 채택하지 않습니다. 확인된 서류가 없으면 빈 섹션을 생략하며 서류가 없다고 단정하지 않습니다. 확인되지 않은 자격·기한·접수·문의·서류는 사용자용 `안내:` 한 줄로 묶고 내부 체크리스트 오류 문구를 그대로 노출하지 않습니다. 명시적인 선정/심사/평가 결과 안내에서 별도 이의신청·추가 제출이 없으면 신청 정보 누락 안내를 생략합니다. 확인된 문의처는 계속 표시하며 메일에서는 `문의 안내` 제목을 사용합니다.
- 일반형 최대 6종, 축약형 최대 3종을 표시하고 생략 건수와 사업 제안 체크리스트 또는 원문 확인 안내를 남깁니다. 기존 3,900 UTF-16 표시 문자/20,000 저장 문자 한도를 유지합니다. 저장된 구형 보고서의 카드·첨부 표시는 계속 지원합니다.
- 새 보고서 생성에만 경량 모델을 사용합니다. 기존 저장 보고서는 자동으로 다시 쓰지 않으며 재전송만으로 새 모델이 적용되지 않습니다. 사업 제안 탭 생성 조건과 저장 제안 데이터는 변경하지 않습니다.

### 보고서 한국어 안내와 표시 (2026-09-22)

- 새 모델 출력은 실제 `evidence.quote`, 필요한 경우 원문 순서의 `fragments`, 짧은 한국어 `display_text`로 나눕니다. 검증용 text는 코드가 인용/구절에서 생성하여 모델이 중복 작성하지 않습니다. 구형 추출 객체는 내부 호환을 유지합니다.
- 모델에는 역할·국가별 한 항목, 국문 우선·영문 번역, 필수/예외/동시접수 조건 보존을 지시합니다. 표의 머리글과 값을 역할별로 연결하고 문의처를 제출처로 분류하지 않도록 예시를 제공합니다.
- 한국어 표시문은 최대 150자입니다. 원문 인용 검증 후 숫자·날짜·연락처·URL을 대조합니다. 없는 숫자/연락처 추가와 발췌한 숫자/연락처 삭제, 숫자 뒤 이상/이하/미만/초과 방향 변경을 거부하고 긴 영문 설명을 표시하지 않습니다. 영문 월 이름은 날짜 대조 시 정규화합니다. 이는 완전한 번역·의미·예외 조건 검증이 아니며 모델 오류나 누락 가능성은 남습니다.
- 표시문 검증 실패 시 그 항목의 원문 조각을 다시 화면에 노출하지 않습니다. 모델 실패 시에도 150자를 넘는 긴 원문·영문 설명·깨진 자격 표제는 제외하고 짧은 기존 정보와 원문 링크로 보고서를 유지합니다. 기존 저장 체크리스트의 정확성을 새로 보장하는 처리는 아닙니다.
- 복수 대상·접수처·문의처는 저장 본문에서 `  ↳ ` 줄로 구분하며, 이메일과 웹에서는 각 값이 독립된 줄로 표시됩니다. URL 안의 슬래시로 문장을 나누지 않습니다. 기존 본문 형식도 계속 렌더링합니다.
- 기존 보고서 생성의 1회/공고 호출 안에서 처리하며 추가 번역 호출이나 자동 모델 재시도는 없습니다. 새 프롬프트 버전은 캐시 키를 바꿉니다. 서버 재시작과 새 보고서 생성부터 적용하며 저장 보고서 재전송만으로 문장을 다시 작성하지 않습니다.

### 기본 보고서 선저장과 장애 복구 (2026-09-22)

- 보고서 단계 진입 시 제목·기관·저장 요약(없으면 분석 미완료 안내)·원문 링크로 기본 본문을 만들고 `monitoring_reports`에 PENDING으로 저장합니다. 본문은 URL 포함 약 3,500 UTF-16 단위 이내이며 초과 게시글은 생략 수를 표시합니다. 게시글이 0건이어도 빈 결과 안내 보고서를 만듭니다.
- 같은 트랜잭션에서 `report_tasks`에 보강 입력 JSON을 저장합니다. 사업 제안 완료와 분석 재사용 경로는 BEFORE_COMMIT에 작업을 남깁니다. AFTER_COMMIT의 HTTP 요청/이벤트가 유실돼도 작업은 DB에 남습니다. 수집/분석 등 보고서 단계 이전의 작업 전체를 복구하는 큐는 아닙니다.
- `report_tasks`: task_id PK, report_id 유일 FK, state(PENDING/RUNNING/DELIVERY_PENDING/DELIVERING/DONE), request_json CLOB, attempt_count, attempt_id UUID, available_at, last_error. 상태/실행 가능 시각 인덱스와 전용 시퀀스를 사용합니다. 완료 후 원문을 포함한 요청 JSON은 지웁니다. 기존 보고서 상태 제약/발송 FK는 유지합니다.
- 모델 호출 전 잠금 트랜잭션에서 시도 ID와 2분 임대를 확정합니다. 보강 작업은 최대 3회 요청하며 접수 실패 시 10초 뒤 재시도, 응답 유실/프로세스 중단은 임대 만료 후 재시도합니다. 3회 소진 또는 FAILED 결과 수신 시 이미 저장한 기본 본문으로 COMPLETED 처리합니다. 입력 직렬화 실패도 기본 보고서로 완료합니다. 실제 처리 시각에는 복구 주기와 대기열 지연이 추가됩니다.
- Python 보고서 접수 요청에 `jobId`(UUID, 하위 호환을 위해 생략 가능)를 추가합니다. 서버가 지정한 값은 202 응답과 최종 콜백에서 그대로 사용합니다. 백엔드는 실행 행 잠금과 현재 시도 ID로 콜백을 확인합니다. 늦은 시도·중복·이미 완료된 보고서의 결과에는 duplicate=true로 응답하고 본문과 발송을 다시 변경하지 않습니다. 양쪽 서버를 함께 업데이트해야 합니다.
- 성공한 보강 본문 또는 기본 본문을 확정한 트랜잭션에서 작업을 DELIVERY_PENDING으로 바꿉니다. 발송 전에만 내용을 보강하며, 기본 보고서를 먼저 발송한 뒤 나중에 보강본을 다시 보내지 않습니다. 발송 복구는 채널별 기존 수신자 스냅샷·유일 제약·잠금을 재사용합니다.
- 5초마다 최대 20개 작업을 조회하며 시작 후 최초 10초부터 복구합니다. 생성 중단, 결과 전달의 3회 HTTP 재시도 소진, 발송 이벤트 유실을 임대 만료/대기 작업 재실행으로 처리합니다. 기존 PENDING/FAILED 보고서 중 작업 행이 없고 실행이 COLLECTED인 건도 재등록합니다. 이미 완료된 과거 보고서는 자동 재전송하지 않습니다.
- 자동 발송은 외부 호출 전에 시도 횟수를 별도 트랜잭션으로 확정합니다. 전송 뒤 저장 실패/프로세스 중단이면 예약 기록이 남으므로 자동 재전송하지 않습니다. 2분 이상 미완료인 예약은 수신 여부 확인이 필요한 FAILED로 바꾸고 기존 명시적 재전송 기능을 사용합니다. 외부 SMTP/Telegram이 중복 방지 키를 지원하지 않는 상황에서 정확히 한 번 수신까지 보장하지는 않습니다.
- 발송 준비/DB 오류는 다른 채널 처리를 계속하고 작업을 1분 후 재시도합니다. 아직 PENDING 수신자가 있으면 작업을 종료하지 않습니다. 비활성 채널과 수신자 없음은 발송 대상이 없으므로 완료하며, 이미 기록된 전송 실패를 무조건 재발송하지 않습니다.
- 운영 설정: `app.report.recovery.enabled` 기본 true, interval-ms 5000, initial-delay-ms 10000. 기본 로컬 `ddl-auto=update`는 새 테이블을 생성합니다. validate/none 환경은 [Oracle DDL](sql/report-tasks-2026-09-22.sql)을 먼저 적용합니다. DB 자체가 불능인 동안의 신규 저장/전송, 보고서 단계 이전 수집·분석 실패, 실제 수신함 도착까지 보장하는 변경은 아닙니다.

### 제출 안내 경량 모델과 제한

- `REPORT_BRIEF_ENABLED=true`, `REPORT_BRIEF_MODEL=gpt-5-mini`가 기본이며 기존 `OPENAI_API_KEY`를 사용합니다. JSON Schema 구조화 출력으로 필드·배열 크기를 제한하고 reasoning effort는 minimal, 출력 상한은 5,000토큰, 자동 재시도는 0회입니다.
- 백엔드는 파싱 완료 첨부에 총 120,000자/파일당 40,000자를 균등 배분하고 짧은 파일의 남는 분량을 재배분합니다. 앞쪽 대용량 ZIP이 뒤쪽 안내문의 예산을 모두 소진하지 않습니다. 잘린 첨부에는 생략 표시를 남깁니다.
- 본문·첨부/ZIP 내부 파일의 경계를 유지하며 관련 구절을 합계 16,000자, 최대 40개 출처로 선택합니다. 출처 한도 선택과 분량 배정에서 공고/접수 안내를 우선하며 우선 예산 75%와 남은 분량을 사용합니다. 일반 양식은 600자, 참고자료는 150자까지 선택하되 파일명은 유지합니다. 선택 범위 메타데이터는 입력 구성에 보존하며, 모델에는 이스케이프된 JSON 문자열 대신 실제 줄바꿈과 출처 ID로 전달합니다. 참고자료 본문은 제출 안내 모델에서 제외합니다. upstream의 본문/첨부 입력 절단 및 파싱 실패로 이미 누락된 정보는 복구하지 못합니다.
- 제출 안내 값은 공백·유니코드·따옴표 정규화 후 원문과 대조합니다. 원문에 직접 존재하는 값 또는 실제 인용에 순서대로 존재하는 구절의 조합만 허용하며, fragments도 같은 검사를 받습니다. 서류명·조건은 실제 인용 안에 있어야 합니다. 참고자료는 현재 공고의 제출 안내·서류 근거로 채택하지 않습니다. 날짜 유효성 및 기한에 방법·서류가 섞이는지 추가 검사합니다. 모델의 미지원 값은 버리고 해당 필드의 표제가 있는 원문 구절로 보완합니다. 이 보완에는 기존 모델 분석의 자격·기한 문장을 사용하지 않습니다. 여전히 없는 값은 간단한 원문 확인 안내로 모읍니다. 선행 물결표와 `까지`가 있는 단일 마감일은 허용하지만 끝점이 깨진 범위는 거부합니다. 이는 근거 없는 새 문구를 거르는 장치이며 원문 해석·예외 조건 누락까지 완전 검증하지는 못합니다.
- PDF는 기본 추출에서 문장이 긴 한 줄로 뭉친 페이지에 한해 레이아웃 추출로 다시 읽습니다. 정상 페이지는 추가 추출을 생략합니다. 이미 저장된 파싱 텍스트를 자동 재작성하지 않습니다.
- 구형 추출 출력은 원문 접수 행으로 보완합니다. 한국어 표시문이 있는 새 출력은 검증된 국가별 접수·동시접수 조건을 각각 표시하며 원문 접수 행 전체를 덧붙여 중복시키지 않습니다. 단, 원문에 명시된 한쪽 제출 시 제외 조건이 한국어 안내에서 빠지면 짧은 해당 원문 조건을 함께 보존합니다. 모델이 서류 목록을 반환하지 않으면 접수 절차에 명시된 계획서 등 최소 서류를 양식과 연결하고 전체 제출서류 확인 안내를 남깁니다.
- 양식명 매칭은 확인서·신청자격을 포함하고 신청자격/신청자격 적정성 확인서 같은 표기 차이를 정규화합니다. 다른 신청 절차, 공고문·참고자료 및 복수 후보의 임의 연결은 차단합니다.
- 첨부 URL은 모델 입력/출력 필드로 제공하지 않습니다. 모델은 출처 ID만 지정하고 서버가 수집 첨부명·양식 내용과 대조한 뒤 원래 URL을 연결합니다. 자체 증빙은 링크 없는 이름으로, ZIP 양식은 원본 ZIP 다운로드로 표시합니다.
- 공고 입력당 1회 호출, 작업 내 동일 입력은 호출을 공유합니다. 기본 동시 처리 3건, 개별 30초, 작업 전체 60초 제한이며 시간 초과 시 남은 호출을 취소하고 기존 추출 방식으로 대체합니다. 실패 공고에 정리 실패 안내를 남기고 나머지 보고서는 저장합니다.
- 프로세스 내 LRU 256개/24시간 캐시를 사용합니다. 모델·프롬프트 버전·선택 원문·파일명·저장 체크리스트 변경 시 무효화하고, 캐시 재사용 때도 현재 수집 URL로 다시 연결합니다. 재시작하면 캐시가 사라지며 서로 다른 작업의 동시 캐시 미스까지 중복 제거하지는 않습니다. 비용과 지연은 캐시 미스에서 추가됩니다.
- 설정: `REPORT_BRIEF_TIMEOUT_SECONDS`, `REPORT_BRIEF_TOTAL_TIMEOUT_SECONDS`, `REPORT_BRIEF_CONCURRENCY`, `REPORT_BRIEF_MAX_TEXT_CHARS`, `REPORT_BRIEF_CACHE_TTL_SECONDS`. 키/모델 접근 실패는 비밀정보 없이 오류 유형만 기록하고 대체 결과를 사용합니다. 실제 API 품질·지연 평가는 별도 검수 대상입니다.

### 보고서 발송 화면과 이메일

- `/reports`의 `보고서 발송` 화면에서 텔레그램·이메일 채널을 선택합니다. 기존 `/telegram` 주소는 새 화면으로 이동합니다. 수신자 설정과 채널 발송 스위치는 각각 독립적이며, 발송 이력은 보고서별로 묶어 두 채널의 결과를 함께 표시합니다. 이메일은 Gmail·네이버를 포함한 유효한 메일 주소를 최대 20개 등록합니다.
- 기존 관리자 권한과 운영 공통 설정 범위를 유지합니다. `/api/email/**`와 `/api/report-deliveries/**`는 관리자만 접근합니다. 발신 계정·앱 비밀번호는 서버 설정으로 관리하며 API에는 서비스 종류·발신 주소·이름·설정 여부만 반환합니다. 설정됨은 인증 정보 등록 여부이며 실제 인증 성공이나 수신함 도착을 보장하지 않습니다.
- 발신 서비스는 서버에서 `GMAIL` 또는 `NAVER` 하나를 선택합니다. 각각 `smtp.gmail.com`·`smtp.naver.com`의 587 포트로 인증·STARTTLS·서버 인증서 호스트 검증을 적용합니다. 연결 제한은 3초, 읽기·쓰기 제한은 10초입니다. 발신 주소는 인증 계정과 같고, 수신 서비스는 제한하지 않습니다. 두 발신 계정을 동시에 운용하는 기능은 포함하지 않습니다.
- 이메일은 기본 발송 꺼짐·수신자 없음으로 시작합니다. 계정 미설정 상태에서도 수신자는 저장할 수 있습니다. 낙관적 버전 검사로 다른 화면의 변경을 덮어쓰지 않으며, 주소 앞뒤 공백 제거·소문자 정규화 후 중복을 차단합니다. 수신자 목록의 순서 변경·삭제는 Oracle 고유 제약 충돌을 피하도록 기존 컬렉션을 먼저 비우고 flush한 뒤 같은 트랜잭션에서 저장합니다.
- `email_settings`는 운영 설정 한 건(ID 1, version, enabled)을, `email_recipients`는 순서·이름·주소·수신 여부를 저장합니다. `(settings_id, email_address)`는 유일합니다. `email_deliveries`는 보고서 FK·수신자 스냅샷·PENDING/SENT/FAILED 상태·시도 횟수·시각·안전한 오류 문구를 저장하고 `(report_id, email_address)` 고유 제약 및 별도 시퀀스를 사용합니다. 기존 텔레그램 테이블·이력을 변경하지 않습니다.
- 새 보고서는 완료와 함께 저장한 발송 작업을 실행기가 처리합니다. 작업 행이 없는 구형 완료 이벤트는 기존 채널 리스너가 처리합니다. 채널 준비 오류를 격리하며 이메일은 최대 3개 수신자에게 병렬 전송합니다. 보고서 잠금으로 대상 생성을 직렬화하고 발송 행 잠금·시도 횟수로 중복 완료 이벤트와 동시 재전송을 차단합니다. 전송 직전 현재 채널·수신 설정을 다시 확인합니다. 외부 SMTP 호출은 수신자 발송 행의 개별 트랜잭션에서 진행하며 텔레그램과 같은 행별 잠금 패턴을 사용합니다.
- SMTP 실패는 해당 수신자에만 기록합니다. 실패 재전송은 관리자의 명시적 요청으로 수행하며 예상 주소·시도 횟수·현재 수신자·발송 여부·보고서 완료 상태를 확인합니다. 완료 대상은 재전송하지 않습니다. 결과 저장 실패 시 수신 여부 확인을 안내하고 자동 재시도하지 않습니다. DB 작업 복구를 지원하되, 외부 SMTP 접수 여부가 불확실한 전송은 자동 재전송하지 않으며 정확히 한 번 수신은 보장하지 않습니다.
- 각 이메일은 한 수신자만 To에 넣고 저장된 보고서 제목·본문을 일반 텍스트와 HTML 대안 본문으로 보냅니다. HTML은 원문을 이스케이프하며 첨부파일·읽음 추적은 포함하지 않습니다. `SENT`는 SMTP 서버 접수 성공이며 수신함 배달·열람 확인은 아닙니다. 네트워크 실패 시 이미 접수됐을 가능성을 안내합니다.
- 통합 이력은 기간·상태·채널 기준으로 서버에서 페이징합니다. 모든 채널에서 상태를 필터링하면 해당 상태를 가진 채널이 하나라도 있는 보고서를 표시합니다. 채널 선택은 상태 판정 및 표시 범위를 바꾸며 해당 채널의 미발송 보고서도 조회할 수 있습니다. 기존 텔레그램 단일 발송 이력도 표시하고, 이메일 실패를 보고서 생성 실패로 바꾸지 않습니다.
- 텔레그램·이메일 모두 채널 탭 → 자동 발송 → 받는 사람 순서와 공통 `DeliveryChannelPanel` 컴포넌트를 사용합니다. 자동 발송은 스위치, 개별 수신 여부는 체크박스로 통일하며 `발송 설정`·수신자 추가·테스트·수정·삭제를 같은 위치에 둡니다. 텔레그램에만 필요한 연결 확인은 수신자 행의 추가 기능으로 유지합니다. 채널 탭은 방향키·Home·End로 전환할 수 있습니다.
- 채널 작업 영역은 회청색 바탕, 자동 발송은 파란색 카드, 수신자 목록은 별도 테두리와 회청색 제목 영역으로 구분합니다. 선택한 행에는 연한 파랑 배경·왼쪽 강조선·체크 표시를 함께 적용합니다. 채널 탭의 선택 상태는 파란색 채움으로 표시하고 발송 상태 배지는 반복하지 않습니다. 미설정 안내는 자동 발송의 한 줄 설명과 `발송 설정` 버튼으로 통합하며 별도 배너·하단 안내 줄을 두지 않습니다.
- 이메일 발신 계정이 없더라도 자동 발송 스위치·테스트 버튼은 클릭할 수 있으며 `보내는 계정 설정` 창으로 연결합니다. 이때 발송 설정을 저장하거나 실제 메일을 보내지 않고 꺼짐을 유지합니다. 설정 방법은 창의 관리자용 접힘 영역에 둡니다. `설정 상태 다시 확인`은 최신 설정을 조회해 화면과 상위 수신자 정보를 동기화하며, 계정이 확인돼도 자동으로 켜지지 않습니다. 사용자가 `자동 발송 켜기`를 눌러 서버 저장에 성공해야 켜집니다. 조회·저장 실패는 창 안에 표시하며 기존 상태를 보존합니다.
- 텔레그램도 봇 미설정 상태에서 자동 발송·테스트·연결 확인을 클릭하면 `텔레그램 발송 설정` 창을 엽니다. 이때 저장·발송 요청을 하지 않습니다. 봇 재조회 이후 명시적으로 자동 발송을 켜며 등록 안내·봇 링크 복사는 설정 창 안에 둡니다. 수신자 추가·수정 창에서도 봇 링크와 채팅 ID 안내를 제공합니다.
- 설정 토글과 수신자 체크박스는 서버 저장 성공 후 화면에 반영합니다. 메일 테스트는 발송 꺼짐 상태에서도 저장된 수신자에게 명시적으로 한 건 보낼 수 있습니다. 메일 설정·권한·발송 API와 서버 설정 방법은 [백엔드 실행 안내](../backend/README.md)의 이메일 절을 참고합니다.

개발 DB를 처음부터 다시 준비할 때는 `ddl-auto=create`로 현재 엔티티 기준의 스키마를 생성합니다. 기존 데이터는 초기화되며 별도 마이그레이션 SQL은 필요하지 않습니다. 생성한 데이터를 이후 실행에서 유지하려면 스키마가 준비된 상태에서 `ddl-auto=none`으로 실행합니다.

[백엔드 실행 안내](../backend/README.md) · [AI 모듈 실행 안내](../ai/README.md)

현재 API는 백엔드 Swagger와 AI 모듈의 `/docs`에서 확인할 수 있습니다.


## 로컬 프론트엔드 변경 감지

Windows의 Vite 개발 서버는 Git 전환·파일 교체 후 이전 변환 모듈이 남는 문제를 줄이기 위해 1초 간격의 파일 변경 확인을 사용합니다. 이 설정은 개발 서버에만 적용되며, 주기적인 파일 확인에 따른 CPU·파일 시스템 사용량이 추가될 수 있습니다. 포트는 5173으로 고정하고 이미 사용 중이면 다른 포트로 자동 이동하지 않고 실행을 중단합니다. 브랜치를 전환한 뒤 화면과 코드가 다르면 기존 개발 서버를 재시작하고 브라우저를 새로고침합니다.
