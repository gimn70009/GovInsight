# GovInsight 설계

## 1. 프로젝트 개요

GovInsight는 공공기관 게시판을 주기적으로 확인하고 새로 등록되거나 변경된 문서를 수집·분석하는 모니터링 시스템이다.

주요 목적은 기업에 영향을 줄 수 있는 규제, 지원사업과 공고의 변화를 빠르게 확인하는 것이다.

## 2. 시스템 구성

### Spring Boot

- 사용자 로그인과 JWT 인증
- 모니터링 소스, 실행 이력과 문서 데이터 관리
- Python 작업 요청과 수집·분석 결과 수신
- 문서 변경 감지와 Oracle 저장
- 보고서 조회와 Telegram 전송

### Python

- Spring Boot 작업 요청 접수와 백그라운드 실행
- Playwright 기반 게시글 수집
- 첨부파일 임시 다운로드와 메타데이터 계산
- 첨부파일 텍스트 추출과 AI 에이전트 분석
- 처리 결과를 Spring Boot로 전달

PDF·HWP·HWPX 첨부파일 텍스트 추출, LangChain·LangGraph 기반 문서 분석, 분석 결과의 Spring Boot 전달과 Oracle 저장까지 구현됐다.

### Frontend

- 관리자 로그인
- 모니터링 소스 관리와 수동 실행
- 실행 이력, 감지 문서와 보고서 조회

### Oracle

- 사용자와 모니터링 소스
- 전체·소스별 실행 이력
- 문서, 버전, 첨부파일과 감지 결과
- AI 분석 결과와 실행 보고서

Python은 Oracle에 직접 접근하지 않는다. 데이터 저장과 조회는 Spring Boot가 담당한다.

## 3. 모니터링 처리 흐름

```text
관리자가 실행 요청
        ↓
Spring Boot가 실행 이력 생성
        ↓
Spring Boot가 Python에 작업 요청
        ↓
Python이 작업 ID와 ACCEPTED 상태 반환
        ↓
Python 백그라운드 작업에서 게시글과 첨부파일 수집
        ↓
Python이 Spring Boot에 수집 결과 전달
        ↓
Spring Boot가 변경 감지 후 Oracle에 저장
- 전체 실행 상태 = COLLECTED
        ↓
Python AI 분석과 분석 결과 전달
        ↓
분석과 보고서 생성 완료
- 전체 실행 상태 = COMPLETED
```

## 4. Spring Boot와 Python 통신

### 작업 접수

```http
POST /internal/monitoring/jobs
```

Spring Boot가 실행 ID와 활성 모니터링 소스 목록을 전달한다. Python은 작업을 백그라운드로 예약하고 작업 ID와 `ACCEPTED` 상태를 즉시 반환한다. `ACCEPTED`는 전체 작업 완료가 아니라 접수 완료를 뜻한다.

### 수집 결과 전달

```http
POST /internal/monitoring/collection-results
```

Python은 실행 ID, 작업 ID, 소스별 처리 상태, 게시글과 첨부파일 정보를 전달한다.

Spring Boot는 제목과 본문을 정규화하고 해시를 비교하여 `NEW_DOCUMENT`, `UPDATED_DOCUMENT`, `UNCHANGED_DOCUMENT`를 판정한다. 신규·수정 문서만 새 버전과 첨부파일을 저장하고, 모든 문서는 실행별 감지 결과를 남긴다.

하나 이상의 소스가 정상 저장되면 전체 실행 상태를 `COLLECTED`로 변경한다. 모든 소스가 실패하면 `FAILED`로 변경한다. 수집 완료는 전체 처리 완료가 아니므로 `COMPLETED_AT`은 기록하지 않는다.

### AI 분석 작업 요청과 결과 전달

수집 결과 저장과 변경 감지 트랜잭션이 커밋되면 Spring Boot가 분석 대상을 선별하여 Python에 분석 작업을 요청한다. Python은 Oracle을 직접 조회하지 않으므로 분석에 필요한 현재 문서와 이전 버전 정보는 요청 데이터에 포함한다.

```http
POST /internal/monitoring/analysis-jobs
```

```text
수집 결과 저장 및 커밋
        ↓
분석 대상 문서 선별
        ↓
Python 분석 작업 요청
        ↓
202 Accepted + jobId
        ↓
Python 백그라운드 분석 작업 예약
```

Python은 분석 요청을 검증하고 작업 ID를 발급한 뒤 백그라운드에서 LangGraph 문서 분석 워크플로우를 실행한다. 공고 분석은 LangChain 에이전트가 게시글, 첨부파일, 회사 프로필과 이전 버전 도구 중 필요한 도구를 실제로 선택하고 호출하는 구조를 유지한다. 동일 문서 버전의 재시도에서는 같은 도구 컨텍스트와 결과 캐시를 사용하여 원문 가공을 반복하지 않으며 실제로 호출된 도구만 사용 기록에 남긴다. 신규 문서와 변경 없는 문서의 변경 유불리, 회사 적합도가 낮을 때의 중요도와 제안 섹션 제목처럼 코드로 확정할 수 있는 형식은 모델 전체 호출을 반복하지 않고 일반 코드가 보정한다. 각 시도의 소요시간과 실패 원인을 로그에 남긴다. Python 접수 실패가 이미 저장된 수집 결과를 롤백하지 않도록 수집 저장과 분석 요청을 분리한다.

LLM 분석이 끝나면 Python은 성공 결과와 문서별 실패 결과를 다음 내부 API로 전달한다.

```http
POST /internal/monitoring/analysis-results
```

공고 분석은 이 API로 먼저 저장한다. 이후 사업 제안 대상 문서만 품질을 유지하는 `gpt-5-mini` 기본 모델과 240초 제한으로 별도 생성하며, 완료된 제안은 다음 API로 기존 분석 행의 제안 JSON과 사용 도구를 갱신한다. 사업 제안 대상이 없을 때도 Python은 빈 결과로 후속 처리 완료를 알린다. Spring Boot는 이 완료 신호가 커밋된 뒤에만 최종 보고서를 요청한다. 제출서류 ZIP의 내부 파일명은 코드가 전체 후보로 먼저 수집해 모델 입력에 제공하고, 모델 결과에서 누락된 양식은 실제 제출 대상과 적용 조건을 확인해야 하는 항목으로 보완한다.

```http
POST /internal/monitoring/proposal-results
```

Spring Boot는 `runId`, `detectionId`, `documentId`, `versionId` 관계와 실행 상태를 검증한 뒤 성공 결과를 `DOCUMENT_ANALYSES`에 저장한다. 같은 문서 버전의 결과가 다시 전달되면 중복 저장하지 않는다. 다만 기존 사업제안 JSON의 `preparationSchemaVersion`이 현재 버전 12이 아니면 한 번만 재분석하여 같은 행을 새 결과로 교체한다. 현재 버전이어도 후속 제안 생성의 실패 사유가 기록된 결과는 다음 모니터링 실행에서 다시 분석하며, 정상 생성된 뒤에는 기존 결과를 재사용한다. 사업 제안의 지원 조건과 제출 자료는 항목별로 원문 인용을 검증하고, 검증되지 않은 항목만 제외한다. ZIP 내부 문서명도 추출 텍스트의 파일 구분자를 통해 실제 출처로 인정한다. 제안 전략은 최종 참여 판단, 조건부 추천 역할과 대안 역할, 확인된 사실과 AI 해석, 세 목록에서 선택한 핵심 항목, 공고 필수조건과 AI 내부 권장 기준이 분리된 중단 조건을 도출한다. 사용자용 문장은 하나의 완전한 문장과 정중한 합니다체를 사용하고, 세미콜론과 GO 같은 내부 상태 표현 및 정보 구분용 가운데점은 저장 전에 정규화한다. 체크리스트 제목에서는 양식 번호를 제거하되 원본 파일명과 근거 위치에는 그대로 보존한다.

사업 제안 후속 호출은 게시글 본문과 첨부파일에 각각 별도 최대 길이를 적용하지 않고 하나의 32,000자 상한을 공유한다. 신청, 자격, 제출서류, 평가, 컨소시엄과 협약 관련 원문 구간을 우선 선별하고 제출 양식 계열 첨부파일을 먼저 배치한다. 원본 추출 텍스트는 삭제하거나 축약 저장하지 않으며, 생성 결과의 인용 검증에는 전체 원문을 사용한다. 호출 시작 시 입력 길이를, 응답 완료 시 소요시간을 로그에 기록한다.

네트워크 오류와 서버 오류는 설정된 횟수만큼 재시도하고, 요청값이나 관계 오류를 의미하는 4xx 응답은 재시도하지 않는다. 분석 결과 저장만으로 전체 실행을 `COMPLETED`로 변경하지 않으며, 보고서 생성까지 끝난 후 완료 상태로 전환한다.

먼저 확인할 핵심 항목은 회의 안건·지원 조건·제출 서류의 기존 내용을 최대 4개 선택해 표시한다. 각 목록의 첫 우선 항목을 하나씩 포함하고 남은 자리는 신청 단계, 필수·조건부·내부 권장·선택, 회사 증빙 미확인 순으로 채운다. 회의 안건은 생성된 순서를 유지한다. 별도 모델 출력으로 보완 행동을 만들지 않으며 화면 요약에서 원래 목록을 펼쳐 볼 수 있다. 목록에 없는 담당자와 내부 완료 목표일은 화면에 새로 추정해 표시하지 않는다. 준비 체크리스트는 상태를 판정하거나 상태 뱃지를 제공하지 않으며, 원문 요구사항·현재 확인된 근거·다음 행동만 전달한다. 내부 준비도 계산이 필요한 경우 회사 근거 수준을 조건 점수로 사용하고 조건 점수 80%와 일정 여유 20%를 반영한다. 공식 증빙이나 사용자 확인이 없는 항목을 완료 또는 충족으로 간주하지 않는다. 일정 여유는 공고 마감일까지 주말을 제외한 남은 일수와 작업 유형별 고정 준비 기간을 비교하며, 보고서 호환용 핵심 항목의 내부 목표일은 고정 준비 기간과 최종 검수 및 제출 여유 5일을 마감일에서 역산한다. AI는 소요일 숫자를 정하지 않고 작업 유형만 분류하며, 코드는 내부 확인 2일, 외부기관 확인 7일, 공식 문서 발급 5일, 인증 취득 또는 갱신 15일, 서명 및 직인 3일, 예산 검토 7일, 제안서 작성 10일, 기술기획 10일, 국내 파트너 확보 15일, 해외 파트너 확보 25일, 법무 및 계약 검토 7일, 기타 준비 5일의 공통 정책을 적용한다.

사업 제안 탭은 분석이 완료된 모든 문서에 표시한다. 신청서식이나 사업계획서 양식이 없어 제안 대상이 아니거나 회사 적합도, 자격, 접수기한 등의 생성 조건을 충족하지 못하면 준비안을 만들지 않고 같은 탭에서 생성하지 않은 이유를 안내한다. 내용 분석이 완료된 첨부파일명에 신청서식, 제출서식, 사업계획서, 연구개발계획서 또는 제안서 양식이 명시되면 모델 분류와 관계없이 제안 검토 공고로 보정한다.

핵심 항목은 연한 중립 배경의 별도 영역 안에 흰색 카드로 묶어 참여 방식 카드와 시각적으로 구분한다. 영역 제목과 설명은 카드 본문의 왼쪽 선에 맞춘다. 사업 제안 준비 화면은 제안 전략과 먼저 확인할 핵심 항목을 먼저 노출하고, 현황 바로가기에서 회의 안건·지원 조건·제출 서류의 항목 수를 요약한다. 긴 체크리스트 섹션과 항목별 판단·원문 근거는 사용자가 필요할 때 펼쳐 보도록 하여 정보는 유지하면서 초기 스크롤 길이를 줄인다.

사업 제안 모델은 한 번만 호출한다. 후처리 코드는 특정 기관명, 국가명, 사업명 또는 고정된 기관 수를 사용하지 않고 게시글과 첨부 원문에서 숫자가 포함된 기관 구성, 복수 기관·국가의 동시 신청, 인정서·인증서 보유, 설립·사업 운영 기간처럼 문장 자체로 확정할 수 있는 고신뢰 신청 요건을 추출한다. 모델 체크리스트가 인용한 원문과 정규화한 문장, 숫자와 핵심 토큰을 대조하고 연결되지 않은 요건만 `확인 필요` 상태로 보완한다. 원문 위치와 문장을 확보하지 못하거나 의미가 불명확한 조건은 자동 추가하지 않는다. 후보 수, 기존 반영 수와 보완 수는 문서별 로그로 기록한다.

사업 제안 후속 모델 입력은 관련 원문 최대 32,000자와 요약된 1단계 분석만 사용한다. 전체 원문 저장과 최종 인용 검증은 원본을 유지하되, 모델 입력을 구성할 때 게시글·PDF·HWP 사이에서 숫자가 같고 길이와 5글자 묶음 유사도가 높은 반복 문단은 우선 출처 한 곳만 남긴다. 짧은 표 항목과 서로 다른 숫자·조건은 제거하지 않으며 제거 전후 첨부 원문 길이를 로그에 기록한다. 원문 인용 검증과 제출 양식 누락 보완은 전체 저장 원문을 사용하는 일반 코드에서 계속 수행하며, 모델은 최소 추론 강도와 추론 토큰을 포함한 12,000 출력 토큰 상한을 사용한다. 모델 출력 스키마에는 의미 판단에 필요한 상태, 근거, 행동과 작업 유형만 포함하고, 준비도 점수, 세부 점수, 고정 준비 기간, 내부 목표일과 산식 설명은 모델 출력에서 제외한 뒤 공통 코드가 채운다. 응답 로그에는 입력·출력·전체 토큰 수와 소요시간을 남겨 문서별 병목을 측정한다. 모델 오류를 사용자 화면에 표시할 때는 내부 토큰 사용량과 SDK 객체를 제거한 안전한 사유만 저장한다.

## 5. 실행 상태

### 전체 실행 상태

- `REQUESTED`: Spring Boot가 실행 이력을 생성함
- `ACCEPTED`: Python이 작업을 접수함
- `RUNNING`: Python이 수집·처리 중임
- `COLLECTED`: 수집 결과 저장이 끝나 AI 분석을 기다림
- `COMPLETED`: AI 분석과 보고서 생성을 포함한 전체 작업이 완료됨
- `FAILED`: 작업이 실패함

### 소스별 실행 상태

- `PENDING`
- `RUNNING`
- `COMPLETED`
- `FAILED`

### 실행 방식

- `MANUAL`: 관리자 수동 실행
- `SCHEDULED`: 자동 스케줄 실행

자동 실행 설정은 Oracle의 `MONITORING_SCHEDULES` 단일 설정 행에 활성 여부, 실행 주기, 한국 시간 기준 실행 시각, 직접 선택 요일과 마지막 실행 시도일을 저장한다. 관리자는 모니터링 화면에서 `매일`, `평일(월~금)`, `요일 직접 선택` 중 하나를 선택하고 시·분 단위 실행 시각을 지정할 수 있다. Spring Boot 스케줄러는 `Asia/Seoul` 기준으로 매분 설정을 확인하며 같은 설정으로는 같은 날짜에 한 번만 실행을 시도한다. 활성 여부, 주기, 요일 또는 실행 시각이 실제로 바뀌면 마지막 시도일을 초기화하여 같은 날에도 새 일정으로 다시 실행할 수 있다. 실행 시점에 `REQUESTED`, `ACCEPTED`, `RUNNING`, `COLLECTED` 상태의 모니터링이 있으면 새 자동 실행을 만들지 않고 해당 회차를 건너뛴다. 자동 실행은 활성화된 모든 소스를 대상으로 기존 수동 실행 파이프라인을 `SCHEDULED` 방식으로 재사용한다.

```http
GET /api/monitoring-schedule
PUT /api/monitoring-schedule
```

## 6. 모니터링 소스와 지원 범위

현재는 사전에 검증한 다음 6개 기관의 게시판을 우선 지원한다.

- 산업통상부
- 과학기술정보통신부
- 기후에너지환경부
- 고용노동부
- 국토교통부
- 한국산업기술진흥원

모든 공공기관 URL의 자동 수집을 보장하지 않는다. 검증된 게시판은 기본 모니터링 소스로 제공하며, 신규 기관은 Python의 `SiteProfile`과 관련 테스트를 추가하여 지원한다. 사이트 구조가 변경되면 해당 프로필과 테스트도 수정한다.

한국산업기술진흥원 사업공고는 AJAX 목록의 `contentsView` 링크와 상세 페이지의 제목·본문·공고일·첨부파일을 지원한다. 과거의 특정 공고를 재현 테스트할 때는 `boardContentsView.do` 상세 URL 자체를 모니터링 소스 URL로 등록할 수 있으며, 이 경우 해당 문서 한 건만 수집한다.

`listUrl`은 게시글 목록 페이지 주소다. `urlIncludePattern`은 목록에서 상세 게시글 URL만 선택하기 위한 기술 설정으로, 기본 소스에는 검증된 값을 미리 등록한다. 패턴이 없으면 메뉴·로그인·검색 링크를 게시글로 오인할 수 있으므로 신규 소스는 실제 URL 구조를 확인해야 한다.

한 소스의 수집이 실패해도 다른 소스의 수집은 계속한다.

## 7. 문서 수집과 첨부파일 처리

Python은 Playwright로 다음 정보를 수집한다.

- 게시글 제목, 본문과 게시일
- 원문 URL과 원본 게시글 번호
- 첨부파일 이름과 다운로드 URL

기관별 제목·본문·게시일 선택자, JavaScript 링크 변환과 게시글 ID 규칙은 `SiteProfile`에서 관리한다. `read.do`, `view.do`, `DTL.jsp` 같은 페이지 파일명은 게시글 ID로 저장하지 않으며 유효한 식별자가 없으면 `NULL`로 저장한다.

첨부파일은 운영체제 임시 폴더에 스트리밍 방식으로 다운로드한다. 다운로드 중 MIME 타입, 파일 크기와 SHA-256 해시를 계산하고 임시 파일은 즉시 삭제한다. 원본 파일 자체는 Oracle에 저장하지 않는다.

파일명 뒤에 `[123 KB]`, 쉼표 또는 보이지 않는 문자가 붙어도 정규화한 확장자를 기준으로 파서를 선택한다.

- PDF·HWP·HWPX와 ZIP 내부의 PDF·HWP·HWPX 파싱 성공: `EXTRACTED_TEXT` 저장 및 `PARSE_STATUS=COMPLETED`
- PDF·HWP·HWPX 파싱 실패: `PARSE_STATUS=FAILED`와 안전한 오류 메시지 저장
- 그 외 형식: `PARSE_STATUS=UNSUPPORTED`
- 다운로드 실패: `PARSE_STATUS=FAILED`와 안전한 오류 메시지 저장
- 제한 시간과 최대 파일 크기: 환경변수로 관리
- HWPX: ZIP 내부 `Contents/section*.xml`을 순서대로 읽어 본문 텍스트 추출
- ZIP: 경로에 직접 압축 해제하지 않고 내부 PDF·HWP·HWPX만 임시 파일로 스트리밍하여 기존 파서로 처리. 내부 파일은 기본 20개, 총 압축 해제 크기는 기본 100 MiB로 제한하고 중첩 ZIP은 분석하지 않음. 일부 문서 파싱에 실패해도 성공한 문서의 텍스트는 파일명과 함께 저장
- PDF: `pypdf`로 페이지 순서대로 텍스트를 추출하고 페이지 사이를 줄바꿈으로 구분
- HWP: `olefile`로 HWP 5.0 OLE 구조를 열고 `FileHeader`를 검사한 뒤 `BodyText/Section*`의 문단 텍스트 레코드를 추출. UTF-16 surrogate pair는 하나의 문자로 결합하고 잘못된 단독 surrogate는 대체 문자로 정규화
- 압축된 HWP 본문은 raw deflate 방식으로 압축 해제하며 암호화·DRM·배포용 HWP와 그림·OLE 개체 추출은 지원하지 않음
- 이미지로만 구성된 PDF와 암호화된 PDF는 OCR 없이 실패 처리

## 8. 문서 저장과 변경 감지

### 주요 테이블

- `DOCUMENTS`: 게시글 고유 정보와 최초·마지막 발견 시각
- `DOCUMENT_VERSIONS`: 제목, 본문, 게시일과 첨부파일 구성이 변경된 버전
- `DOCUMENT_ATTACHMENTS`: 버전별 첨부파일 메타데이터와 파싱 결과
- `DOCUMENT_DETECTIONS`: 실행별로 감지된 문서, 버전과 변경 유형

```text
MONITORING_SOURCES 1 ── N DOCUMENTS
DOCUMENTS 1 ── N DOCUMENT_VERSIONS
DOCUMENT_VERSIONS 1 ── N DOCUMENT_ATTACHMENTS

MONITORING_RUN_SOURCES 1 ── N DOCUMENT_DETECTIONS
DOCUMENTS 1 ── N DOCUMENT_DETECTIONS
DOCUMENT_VERSIONS 1 ── N DOCUMENT_DETECTIONS
```

JPA 연관관계는 자식 엔티티의 `LAZY @ManyToOne` 단방향 매핑을 기준으로 한다.

`DOCUMENT_DETECTIONS`는 실행별 감지 사실과 변경 유형만 저장한다. 수집 순서는 저장하지 않는다. 감지 문서 조회는 기본적으로 감지 시각과 감지 ID의 내림차순을 사용하며, 사용자가 기회 점수순을 선택하면 기회 점수의 내림차순으로 정렬한다. 이때 분석 전이어서 기회 점수가 없는 문서는 점수가 있는 문서 뒤에 배치하고, 점수가 같으면 감지 시각과 감지 ID의 내림차순을 보조 기준으로 사용한다.

변경 유형은 다음과 같다.

- 처음 발견한 게시글: `NEW_DOCUMENT`
- 기존 게시글의 내용이나 첨부파일이 변경됨: `UPDATED_DOCUMENT`
- 이전 버전과 같음: `UNCHANGED_DOCUMENT`

제목, 정규화한 본문, 첨부파일 이름·다운로드 URL·파일 SHA-256 해시로 버전 해시를 생성한다. 따라서 첨부파일의 이름과 URL이 같아도 실제 파일 내용이 바뀌면 수정 문서로 판정한다. 변경되지 않은 문서는 새 버전을 만들지 않고 마지막 확인 시각만 갱신한다.

## 9. AI 에이전트

수집, 첨부파일 파싱과 변경 감지는 예측 가능한 일반 코드가 담당한다. 분석 대상은 다음 규칙으로 선별한다.

- `NEW_DOCUMENT`, `UPDATED_DOCUMENT`: 현재 문서 버전을 분석한다.
- `UNCHANGED_DOCUMENT`: 해당 버전의 기존 분석 결과가 있으면 재사용한다. 단, 제안서 제출 공고의 기존 결과에 현재 요구사항 근거 스키마가 없으면 한 번만 재분석한다.
- `UNCHANGED_DOCUMENT`: 기존 분석 결과가 없으면 누락 보완을 위해 분석을 요청한다.

모든 감지 문서가 기존 분석을 재사용하여 새 분석 대상이 0건인 경우에는 Python 분석 작업을 생략하고, 기존 분석 결과를 모아 곧바로 실행 보고서 생성을 요청한다. 따라서 변경 없는 실행도 보고서 저장 후 `COMPLETED`로 전환된다.

### 분석 입력

Spring Boot는 저장이 완료된 문서 버전을 기준으로 다음 정보를 Python에 전달한다.

- 실행 ID와 문서 버전 ID
- 변경 유형, 기관명과 게시판명
- 제목, 게시글 본문, 게시일과 원문 URL
- 파싱에 성공한 첨부파일의 이름과 추출 텍스트
- 수정 문서인 경우 바로 이전 버전의 제목과 본문
- 수정 문서인 경우 직전 버전에 저장된 AI 분석 결과와 제안 방향
- 공식 홈페이지와 공개 기업 정보로 구성한 BISTelligence 회사 프로필은 Python의 분석 컨텍스트에서 제공

게시글 본문과 첨부파일 텍스트가 모두 비어 있으면 분석하지 않고 실패 사유를 기록한다. 입력이 모델의 허용 범위를 넘으면 제목과 게시글 본문을 우선하고, 첨부파일은 파일 순서대로 제한한다. 정확한 길이 제한은 사용할 모델을 확정할 때 설정값으로 관리한다.

### 분석 결과

문서 버전 하나에 대해 다음 결과를 생성하며 `DOCUMENT_ANALYSES`에 저장한다.

- `SUMMARY`: 문서의 전체 요약
- `KEY_POINTS`: 핵심 내용 목록
- `IMPORTANCE`: `HIGH`, `NORMAL`, `LOW` 중 하나
- `REASON`: 중요도 판정 근거
- `ELIGIBILITY`: `ELIGIBLE`, `INELIGIBLE`, `REVIEW_REQUIRED` 중 하나
- `FAVORABLE_OR_NOT`: 수정 내용이 회사에 미치는 유불리. 신규 문서는 `NOT_APPLICABLE`
- `PROPOSAL_DIRECTION`: `sections[{title, body}]` 형태의 회사 활용·대응 전략 JSON. 수정 시 이전 방향 대비 변화를 포함
- `OPPORTUNITY_SCORE`: 다섯 기회 지표의 고정 가중치로 계산한 0~100 종합 점수
- `OPPORTUNITY_ASSESSMENT`: 지표별 점수와 짧은 판단 근거 JSON
- `USED_TOOLS`: 실제 사용한 분석 도구 목록
- `MODEL_NAME`: 분석에 사용한 모델 이름

`SUMMARY`는 고정된 문장 수나 줄 수로 제한하지 않고 문서의 정보량에 맞춰 충분히 작성한다. 화면 메타데이터는 반복하지 않으며, 관련 내용을 주제별 짧은 문단으로 나누고 문단 사이에 줄바꿈을 넣어 가독성을 확보한다. 제목·불릿·번호·마크다운 같은 표현 형식은 저장하지 않고 프론트엔드가 담당한다. `PROPOSAL_DIRECTION`은 프론트가 문자열을 추측해 분리하지 않도록 제목과 본문을 가진 `sections` 배열 JSON으로 저장한다. 기존 일반 문자열 데이터는 상세 API에서 `기존 제안 방향` 단일 섹션으로 변환한다. `KEY_POINTS`는 문자열 배열로 제공한다. 공고 분석의 회사 관점 영역은 아래 공고 포인트 규칙을 사용하며 사업 추진 전략을 생성하지 않는다.

요약과 핵심 내용에는 원문에서 확인할 수 있는 대상, 지원·규제 내용, 신청 또는 대응 기한, 금액과 제출 방법을 우선 포함한다. 원문에 없는 사실을 추정해 확정적으로 작성하지 않는다.

AI가 생성하는 요약, 핵심 내용, 판단 근거, 제안 본문과 기회 점수 근거는 정중한 `합니다체`로 통일한다. 행동 제안도 명령형보다 `~을 권장합니다`, `~할 필요가 있습니다` 형태를 사용한다. 프론트엔드의 안내 문구는 친절한 `해요체`를 유지한다.

회사 프로필은 공식 홈페이지의 사업·기술·적용 산업·사례 정보와 잡플래닛 공개 기업 정보의 설립일·기업 규모·임직원 수·매출액을 구분해 보관한다. 데이터 컨설팅·수집·통합·모니터링·시스템 운영 등 실제 제공 서비스와 수행 분야는 프로필에 유지하되, 해당 범용 역량만으로 제조 AI 에이전트 사업의 적합도 점수를 부여하지 않는다. 공개 기업 정보는 적합성 판단의 참고 자료로만 사용하며 중소기업확인서, 인증·특허, 자본금과 공고별 공식 증빙은 별도 확인이 필요하다. 출처와 근거 한계를 분석 컨텍스트에 함께 제공하고, 확인되지 않은 자격 조건은 추측하지 않고 `REVIEW_REQUIRED`로 판정한다.

### AI 기회 점수

AI는 회사 적합도, 사업 매력도, 실행 가능성과 긴급도를 각각 0~100 정수와 사용자용 근거로 생성한다. 네 항목은 누락과 중복 없이 각각 한 번씩 포함해야 하며 Pydantic과 Spring 요청 검증에서 확인한다. 점수 산식은 내부 판단에만 사용하고, 화면에 표시되는 근거는 회사가 이 공고를 어떻게 해석하고 무엇을 확인하거나 준비해야 하는지 한두 문장의 자연스러운 `합니다체`로 설명한다. 화살표, 내부 필드명, 세부 점수 나열과 `총 N점` 표현은 표시하지 않는다.

종합 점수는 모델이 직접 정하지 않고 Spring Boot가 다음 고정 가중치로 계산한다.

- 회사 적합도 `COMPANY_FIT`: 50%
- 사업 매력도 `BUSINESS_VALUE`: 20%
- 실행 가능성 `FEASIBILITY`: 20%
- 긴급도 `URGENCY`: 10%

회사 적합도는 핵심 산업 40점, 핵심 AI 과업 40점, 수행 사례 15점, 적용 공정·수요처 5점을 합산한다. 핵심 산업은 반도체·디스플레이·철강으로 한정하며, 핵심 과업은 제조 AI 에이전트의 설계·개발·구축·사업화로 정의한다. 모니터링·통합·플랫폼·데이터 수집·시각화·자동화·스마트팩토리·디지털 전환 같은 범용 표현은 AI 모델이나 AI 에이전트가 실제 산출물 또는 수행 과업으로 명시되지 않으면 과업 점수를 부여하지 않는다. 핵심 산업만 일치하거나 AI 에이전트 과업만 일치하면 최대 40점, 둘 다 확인되지 않으면 최대 20점으로 제한한다. 두 축이 모두 확인되어야 61점 이상, 같은 핵심 산업의 유사 AI 수행 사례까지 확인되어야 81점 이상을 허용한다. 사업 매력도는 직접 경제가치 35점, 전략 산업 확장 25점, 레퍼런스·재사용 가치 20점, 후속·확장성 20점을 합산하고 홍보·평판만 확인되면 40점을 상한으로 적용한다. 실행 가능성은 신청·입찰 자격 30점, 기술 역량 25점, 유사 실적 20점, 인력·일정 준비도 15점, 행정·파트너 준비 10점을 합산하며 직접 자격이나 필수 파트너가 미확인되면 45점, 명시적 결격 조건이 있으면 20점을 상한으로 적용한다. 확인되지 않은 금액·자격·실적은 추정하지 않는다.

긴급도는 한국 시간 기준 분석일과 가장 먼저 도래하는 회사 행동 기한의 달력 날짜 차이로 고정 점수를 부여한다. 마감이 지났거나 회사 행동이 없으면 0점, 기한 미확인 또는 46일 이상은 10점, 31~45일은 20점, 22~30일은 35점, 15~21일은 50점, 8~14일은 65점, 6~7일은 80점, 3~5일은 90점, 0~2일은 100점이다. 상시 접수이면서 선착순·예산 소진 종료가 명시되면 70점으로 평가한다. 사용자용 근거에는 실제 마감일과 `남은 N일`, 회사에 필요한 대응 수준을 자연스럽게 설명한다. 모델 근거에 실제 마감 날짜가 있으면 일반 코드가 한국 시간 분석일과 다시 비교해 잔여일, 긴급도 점수와 마감 여부를 보정하며 모델이 작성한 `남은 N일`을 그대로 신뢰하지 않는다. 마감일에서 역산한 내부 완료 목표일이 분석일보다 과거이면 분석일 기준 즉시 착수로 조정하고, 접수 자체가 끝난 공고에는 새로운 준비 목표일을 제안하지 않는다. 일반 입찰공고의 7일 기준, 일부 보조사업 공모의 15일 이상 접수기간과 특정조달의 40일 공고기간을 실무 준비 구간의 기준점으로 참고한다.

종합 점수 75점 이상이면서 회사 적합도와 실행 가능성이 각각 50점 이상이면 `HIGH`로 표시한다. 종합 점수 50점 이상이면서 같은 최소 조건을 충족하고 긴급도가 85점 이상인 경우도 `HIGH`로 승격한다. 종합 점수 50~74점 중 이 조건을 충족하지 못하면 `NORMAL`, 그 외는 `LOW` 대응 우선순위로 표시한다. 따라서 마감만 임박하고 회사 적합성이나 실행 가능성이 낮은 문서는 긴급도만으로 우선순위가 승격되지 않는다. 상세 화면은 종합 점수와 우선순위, 회사 적합도·사업 매력도·실행 가능성·긴급도 네 지표의 막대그래프와 근거를 제공한다. 막대는 넓은 상세 화면에서도 식별하기 쉽도록 충분한 높이, 트랙 경계, 은은한 그라데이션과 지표 간 여백을 사용하며 지표명·점수·근거 문장은 일반 본문보다 지나치게 작지 않은 크기로 표시한다. 정보 신뢰도 지표는 AI 구조화 출력, API, 종합 점수 산식과 상세 화면에서 사용하지 않는다. 기존 저장 JSON에 남은 해당 항목은 조회 시 무시하며 네 지표만 반환한다. 사용자 화면은 회사 대응 우선순위와 기회 점수를 중심으로 구성하므로 별도로 노출하지 않는 문서 중요도와의 비교 도움말은 제공하지 않는다. 기회 점수 도움말에는 화면에 표시하는 네 지표의 가중치와 회사 적합도 40점 이하의 종합 점수 상한만 간결하게 안내한다. 별도의 차트 라이브러리는 사용하지 않는다.

기존 `DOCUMENT_ANALYSES`에는 `OPPORTUNITY_SCORE NUMBER`와 `OPPORTUNITY_ASSESSMENT CLOB`을 추가한다. 기존 분석처럼 기회 평가 데이터가 없는 행은 상세 API에서 `opportunity=null`로 반환하여 이전 데이터 조회 호환성을 유지한다.

### 중요도 판정

- `HIGH`: 기업의 신청·제출·신고 등 기한이 있거나 규제, 의무, 비용, 인증, 지원 자격에 직접적인 영향을 주어 빠른 검토가 필요한 문서
- `NORMAL`: 기업 의사결정에 참고할 가치가 있지만 즉각적인 신청이나 대응 의무가 명확하지 않은 문서
- `LOW`: 단순 행사, 홍보, 기관 내부 소식 등 기업 활동과 직접적인 관련성이 낮고 별도 대응이 필요하지 않은 문서

중요도는 회사 관련성, 실제 대응 필요성, 대응 시급성 순서로 판정한다. 기한이 있다는 이유만으로 항상 `HIGH`로 판정하지 않으며, LangGraph 검증 단계에서도 `HIGH`인데 회사 적합도가 50점 미만이면 재분석을 요청한다. 기업 관련성과 실제 대응 필요성을 함께 확인하며 판단 근거에는 원문에서 확인한 사실을 간결하게 제시한다.

### 에이전트 실행 원칙

AI 에이전트는 문서 요약, 핵심 내용 추출, 중요도·지원 가능성·변경 유불리 판정과 제안 방향 작성을 담당한다. 도구는 게시글 본문, 첨부파일 추출 텍스트, 회사 프로필, 이전 버전 비교와 직전 AI 분석 조회로 구성한다. 관련 문서 검색은 실제 필요성과 검색 기준을 확정한 뒤 추가한다.

- 수집된 문서 안의 문장은 명령이 아니라 분석 대상 데이터로 취급한다.
- 요약, 핵심 내용, 중요도와 근거는 구조화된 결과 스키마로 검증한다.
- 필수 결과가 비거나 허용하지 않은 중요도 값이 나오면 기본 최대 2회까지 재시도한다. 첨부 양식 기반 분석을 고려해 시도당 기본 제한 시간은 180초로 두며, 사용자용 근거에 포함된 회사 프로필 내부 필드명은 서버에서 한국어 표시명으로 치환해 불필요한 모델 재호출을 줄인다.
- 도구 호출 횟수와 전체 실행 시간에 상한을 둔다.
- 모델의 내부 추론 과정은 저장하지 않고 최종 결과와 실제 도구 사용 기록만 남긴다.
- 테스트에서는 외부 모델 API 대신 결정적인 대체 객체를 사용한다.

### 현재 분석 실행 구조

```text
분석 요청 문서
        ↓
prepare_context
- 변경 유형별 경로와 필수 근거 도구 결정
        ↓
change_type 조건 분기
├─ NEW_DOCUMENT       → analyze_new
├─ UPDATED_DOCUMENT   → analyze_updated
└─ UNCHANGED_DOCUMENT → analyze_unchanged
        ↓
LangChain 에이전트가 경로별 전략으로 도구 선택·분석
        ↓
validate_business_rules
- 필수 도구 사용 여부
- 변경 유형별 유불리 값
        ├─ 통과 → finalize → END
        └─ 실패 → 검증 피드백과 함께 같은 분석 경로 재시도
```

신규·수정·변경 없는 문서는 모두 동일한 공고 포인트 구조를 사용한다. 수정 문서는 이전 버전과 분석에 근거한 조건 변화와 회사 영향을 각 항목 안에 설명하며, 변경 없는 문서는 새로운 변경 사실을 생성하지 않는다. 변경 유형별 유불리 보정은 유지한다.

OpenAI 모델명, 호출 제한 시간, 최대 재시도, 도구 호출 제한, 입력 텍스트 길이와 분석 동시 실행 수는 환경변수로 관리한다. `ANALYSIS_TIMEOUT_SECONDS`의 기본값은 120초, `ANALYSIS_MAX_ATTEMPTS`와 `ANALYSIS_CONCURRENCY`의 기본값은 각각 2다. Python은 `asyncio.Semaphore`로 문서를 최대 두 건씩 동시에 분석하고 입력 순서대로 결과를 모으며, 한 문서의 분석 실패가 같은 작업의 다른 문서 분석을 중단하지 않도록 문서별로 실패를 격리한다. 검증된 분석 결과는 Spring Boot로 전달해 문서 버전별로 Oracle에 저장한다. 성공 또는 기존 저장 결과가 한 건도 없으면 Spring Boot는 빈 보고서 생성을 요청하지 않는다.

Windows의 애플리케이션 제어 정책이 서명되지 않은 네이티브 확장 모듈을 차단하지 않도록 AI 모듈은 프로젝트 소유의 순수 Python 호환 패키지를 우선 사용한다. `uuid_utils.compat.uuid7` 호환 계층은 LangChain과 LangSmith가 사용하는 호출 규격과 UUIDv7의 버전·variant·시간 순서 특성을 유지한다. `xxhash` 호환 계층은 LangSmith와 LangGraph가 요구하는 결정적 128비트 digest와 hexdigest API를 표준 라이브러리로 제공한다. 두 계층 모두 외부 패키지의 다른 API에는 의존하지 않는다.

## 10. 운영 원칙

- 비밀번호, JWT와 API 키를 Git에 저장하지 않는다.
- 로컬 개발 중 Python 서버는 `127.0.0.1`에 바인딩한다.
- 게시글 본문 전체와 민감정보를 로그에 남기지 않는다.
- 외부 요청에는 연결 및 응답 타임아웃을 적용한다.
- 대상 사이트의 이용정책, 저작권 정책과 `robots.txt`를 검토한다.
- 로그인, CAPTCHA와 접근 제한을 우회하지 않는다.
- 과도한 요청을 피하고 수집 주기와 건수를 제한한다.
- 현재는 프로세스 내부 백그라운드 작업을 사용하며, 필요할 때 Redis·Celery 등의 도입을 검토한다.
Python의 사업 제안 후속 처리가 성공·대상 아님·검토 필요 상태로 모두 종료되고 결과 전달 트랜잭션이 커밋되면 Spring Boot는 실행별 최종 분석 결과를 모아 Python에 보고서 생성을 요청한다.

```http
POST /internal/monitoring/report-jobs
```

Python은 작업 ID와 `202 Accepted`를 즉시 반환한 뒤 추가 모델 호출 없이 저장된 문서별 최종 분석 결과를 템플릿으로 조합한다. 제목은 `[공공기관 모니터링] M월 D일 보고서` 형식을 사용한다. 본문은 신규·수정·변경 없음 건수, `🔴`로 구분한 문서 제목, 문서 유형, 기관, 접수 마감, 최종 참여 판단, 기회 점수, 요약, 최대 세 건의 우선 조치와 `📎 원문` URL을 포함한다. 기회 점수가 높은 문서를 먼저 배치한다. Telegram 단일 메시지 제한을 지키도록 제목을 포함해 4,096자 이내로 생성하며, 문서가 많으면 같은 메시지 안에서 공고별 내용을 축약한다.

```http
POST /internal/monitoring/report-results
```

Spring Boot는 실행당 하나의 `MONITORING_REPORTS` 행을 관리한다.

- 요청 전 `PENDING` 보고서를 생성해 콜백과의 경합을 방지한다.
- 성공 결과에는 제목, 요약과 생성 완료 시각을 저장한다.
- 실패 결과에는 오류 메시지를 저장하고 실행 상태는 `COLLECTED`로 유지한다.
- 같은 완료 결과가 다시 전달되면 기존 보고서를 덮어쓰지 않고 중복 응답으로 처리한다.
- 보고서 저장이 완료된 경우에만 전체 실행을 `COMPLETED`로 전환한다.

수동·자동 실행의 최종 보고서 저장 트랜잭션이 커밋되면 Spring Boot가 Telegram Bot API로 제목과 본문을 한 메시지로 전송한다. 따라서 자동 실행도 수집, 기본 AI 분석, 사업 제안과 최종 보고서 생성이 모두 끝난 뒤에만 발송된다. 봇 토큰과 채팅 ID는 `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` 환경변수로만 받고 Git에 저장하지 않는다. `TELEGRAM_ENABLED=true`일 때만 전송하며 연결·응답 제한 시간을 적용한다. 성공한 Telegram 메시지 ID와 전송 시각, 실패 시 안전한 오류 사유를 `MONITORING_REPORTS`에 저장하고 이미 전송 시각이 있는 보고서는 다시 보내지 않는다.

## 11. 목록 조회 API

### 유사 공고 비교

공고 분석의 구조화 응답이 생성된 뒤 긴급도 근거·점수, 변경 유불리, 기회 근거의 내부 표현, 제안 섹션 개수와 제목은 재호출 사유로 사용하지 않고 코드가 결정론적으로 보정한다. 마감 근거가 없으면 `기한 미확인`과 보수적 점수를 사용하고, 실행 근거가 부족한 섹션은 `원문에서 구체적인 실행 근거를 확인하지 못했습니다.`로 채운다. 필수 본문·회사·첨부·이전 버전 자료가 실제로 누락된 경우와 모델/API 또는 구조화 응답 자체가 생성되지 않은 경우에만 전체 분석을 재시도한다.

법률 위험의 내부 판정 스키마는 모델이 반환한 유형과 상태 문자열을 우선 수용한 뒤 알려진 값만 선별한다. 일부 항목이 잘못되거나 중복되어도 전체 판정을 버리지 않고 최초의 유효한 유형별 결과를 검증하며, 누락 유형은 원문 후보 유무에 따라 안전한 기본 결과로 채운다. 원문 후보는 불릿·체크박스 및 줄바꿈으로 문단을 구분하되 문단 내부 문장과 바로 이어지는 예외·단서는 보존한다. 인용 상한은 3,000자이며 상한으로 잘린 문단은 확정 근거로 표시하지 않는다.

법률 위험 판정용 모델은 후보가 존재하는 유형의 상태, 근거 후보 ID, 조항 해석·실무 영향·확인 사항을 반환한다. 근거 후보의 유형·원문 완전성을 검증한 후 세 해석 문장을 기존 summary에 저장한다. 해석이 없으면 상태별 기본 문장을 사용한다. 후보가 없는 유형을 모델에게 억지로 생성시키지 않아 구조화 응답 실패와 불필요한 출력 토큰을 줄인다.

문서 기본 분석이 끝나면 Python은 별도 모델 호출을 추가하지 않고 기존 공고 분석 구조화 출력에 사용자용 비교 요약을 함께 생성한다. 비교 요약은 사업 목적, 지원 규모, 접수 마감, 신청 자격과 필수 파트너를 완전한 문장으로 제공하며 원문에서 확인하지 못한 값은 추측하지 않는다. Spring Boot는 비교 요약을 `DOCUMENT_ANALYSES.COMPARISON_SUMMARY` CLOB에 저장하고 비교표에서 이 값을 우선 사용한다. 새 필드가 없는 과거 분석만 일반 코드의 원문 구간 추출과 기존 분석값을 fallback으로 사용한다.

Python은 제목, 구조화된 사업 목적, 지원 대상과 협력 구조만 하나의 짧은 비교 프로필로 조합하고 `text-embedding-3-small` 기본 모델로 임베딩을 한 번 생성한다. 제출 서류, 일반 신청 절차, 긴 핵심 내용과 AI 사업 제안처럼 공고마다 반복되는 문장은 임베딩 입력에서 제외한다. Spring Boot는 프로필, 벡터와 모델명을 `DOCUMENT_ANALYSES`에 CLOB로 저장한다. 상세 조회 시에는 외부 API를 다시 호출하지 않고 같은 임베딩 모델로 생성된 다른 문서의 최신 분석 벡터와 코사인 유사도를 계산한다. 의미 검색과 제목·사업 목적의 어휘 검색을 각각 수행한 뒤 RRF로 결합하고, 아래 하이브리드 최종 선택 기준을 적용하여 최대 3건을 반환한다. `2026`, `2026년`, `2026년도`와 같은 연도 표기는 핵심 주제에서 제외한다. 제목이 길게 일치하더라도 의미 유사도 임계값을 우회하거나 점수를 85 이상으로 올리지 않는다. 제목·핵심 합성어 일치는 의미 유사도를 보완하는 근거로만 사용한다. 통합 시행계획과 국가별 접수 안내처럼 문서 유형이 달라도 같은 사업 주제이면 비교할 수 있다. `산업기술국제협력`과 `산업기술국제협력사업`처럼 네 글자 이상의 핵심 합성어가 다른 단어에 온전히 포함된 경우도 같은 주제로 인정한다. 같은 문서의 이전 버전은 제외한다. 다른 모델·차원의 벡터와 임베딩 없는 분석은 의미 점수를 계산하지 않고 엄격한 어휘 전용 경로만 검토한다. 어떤 경로의 기준도 넘지 못하면 결과 없음으로 반환한다. 임베딩 생성 실패는 공고 분석 자체를 실패시키지 않으며 어휘 전용 검색을 사용할 수 있다. 임베딩에는 요청 15초·전체 30초 상한을 둔다.

기본 공고 분석은 계획 모델이 문서 메타데이터를 보고 자격·기한·지원 규모·회사 적합성·변경 검토·제안 전략 중 집중 영역을 선택한다. 서버는 이 계획에 따라 허용된 본문·첨부·회사 정보와 이전 버전 조회를 각각 한 번 이하로 실행하고, 분석 모델이 수집된 근거로 구조화 결과를 생성한다. 계획 실패 시에는 보수적인 기본 계획을 사용하며 같은 문서의 재시도에서는 계획을 캐시한다. 자유 순환은 허용하지 않고 `계획 → 제한된 근거 실행 → 분석 → 검증 → 필요 시 1회 수정`으로 종료를 보장한다. 법률 위험 다섯 항목은 일반 코드가 전체 원문에서 본문과 첨부를 번갈아 탐색하여 유형별 최대 여섯 개의 후보 문단을 먼저 선별한다. 후보가 하나라도 있을 때만 같은 모델의 작은 구조화 호출이 실제 의미를 판정하며 기본 공고 분석과 병렬 실행한다. 모델은 부동산 소유·담보 제한과 성과물·지식재산을 구분하며, 명시적 금지·제외·환수·제재는 RESTRICTION_FOUND, 사전 승인·권리 확인·보호 의무는 CAUTION, 후보가 해당 위험을 뜻하지 않으면 NOT_FOUND로 분류한다. 결과 인용이 후보 원문과 일치하지 않으면 코드는 확정 판정을 거부하고 ASSESSMENT_INCOMPLETE로 저장한다. 후보가 없으면 추가 호출을 생략하며, 후보 판정 호출이 실패해도 기본 분석은 유지하고 후보가 있는 항목만 ASSESSMENT_INCOMPLETE로 저장한다. 입력 상한을 넘는 원문은 같은 문자 예산 안에서 앞부분 70%와 끝부분 30%를 보존하여 공고 뒤쪽의 마감·제외·제출 조건이 일괄 유실되지 않게 한다. 기본 분석이 시간 초과 또는 구조화 응답 실패인 경우 두 번째 시도에서만 도구 원문 상한을 24,000자로 낮추며, 첫 시도는 기존 전체 상한을 유지한다. 문서 유형, 회사 적합도와 신청 가능성으로 확정할 수 있는 초안 상태는 모델 응답을 폐기하지 않고 코드가 정책에 맞게 보정한 뒤 검증한다.

유사 공고 조회 시 Spring Boot는 두 공고의 다섯 항목을 결정론적으로 합산한다. 어느 한쪽이라도 명시적 제한이 있으면 RESTRICTION_FOUND로 표시한다. 이는 조항 발견이며 실제 두 사업의 충돌이나 기업의 위반 판정이 아니다. 그 외에는 판정 미완료, 자료 부족, 양쪽 모두 제한 미발견, 확인 필요 순서로 ASSESSMENT_INCOMPLETE, DATA_INSUFFICIENT, NOT_FOUND, REVIEW_REQUIRED를 반환한다. NOT_FOUND는 허용 또는 안전을 뜻하지 않으므로 낮은 위험으로 자동 판정하지 않는다. 새 구조가 없는 과거 분석은 오류를 내지 않고 각 항목을 ASSESSMENT_INCOMPLETE 상태로 반환한다. 백엔드와 프론트엔드의 배포 시점이 달라 응답에 법률 점검 객체가 없는 경우에도 화면을 중단하지 않고 동일한 추가 확인 상태를 표시한다. 결과는 공고별 판단과 원문 인용 및 법률 자문이 아니라는 안내를 제공한다. 항목별 신청 전 확인 문구는 생성하지 않으며 비교 API의 위험 항목에 action 필드를 포함하지 않는다.

임베딩은 표현이 달라도 의미가 비슷한 사업을 찾는 데 사용하며 RAG나 벡터 데이터베이스를 추가하지 않는다. 현재 Oracle 버전에서는 벡터를 JSON CLOB로 보관하고 후보 수가 적은 현재 범위에서 애플리케이션이 코사인 유사도를 계산한다. 화면에는 정확도 백분율이나 통합 점수를 표시하지 않고 `의미·어휘 비교 후보`, `어휘 근거 중심 후보`, `의미 근거 중심 후보`로 검색 근거를 구분한다. 운영 데이터에 정답 라벨을 쌓은 뒤 임계값을 재조정한다.

```http
GET /api/document-detections/{detectionId}/similar-notices
```

상세 화면은 `공고 분석`, `사업 제안`과 분리된 세 번째 `유사 공고 비교` 탭을 제공한다. 세 탭은 전환할 때 패널 폭이 달라지지 않도록 모두 데스크톱에서 최대 1,440px의 동일한 넓은 상세 패널을 사용한다. 탭은 유사 공고 수를 표시하고 여러 건이면 비교 대상을 선택한다. 비교표에는 기관, 사업 목적, 지원 규모, 접수 마감, 신청 자격, 필수 파트너와 양쪽 원문 바로가기를 표시한다. 긴 셀은 표에서 최대 6줄로 정리하되 실제 내용이 넘칠 때만 `전체 보기`를 노출하고, 팝업에서는 저장된 전체 내용을 줄이지 않고 제공한다. `유사한 이유`와 `활용 포인트` 설명 영역은 제공하지 않으며 비교 API에 commonPoints와 proposalReuse 필드를 포함하지 않는다. 그 아래에는 다섯 항목의 `중복·재사용 위험 점검` 영역을 두고 중복되는 종합 상태 배지 없이 요약을 먼저 표시하고, 각 항목의 접기 행에는 항목명과 화살표만 표시하고 상태 문구는 표시하지 않는다. 각 위험은 한 줄 목록으로 접어 두고 사용자가 펼친 항목에만 현재 공고와 유사 공고의 판단을 구분된 영역으로 표시한다. 확인된 원문 인용에는 `원문 근거` 라벨을 붙이고 마지막에 법률 자문이 아니라는 안내를 제공한다. 좁은 화면에서는 종합 상태가 요약 위의 별도 줄로 이동하고 모든 내용은 카드 내부에 유지된다. 기준을 넘는 공고가 없으면 단순 키워드 유사 공고를 제외했다는 빈 상태를 명확하게 안내한다.

관리자 화면의 목록 조회는 0부터 시작하는 `page`와 기본값 20인 `size`를 사용한다. 응답에는 `content`, `page`, `size`, `totalElements`, `totalPages`, `first`, `last`를 포함한다.

### 감지 문서 목록

```http
GET /api/document-detections?page=0&size=20&runId=3&from=2026-08-24T00:00&to=2026-08-24T23:59
```

기본 정렬은 감지 시각과 감지 ID의 내림차순이다. `sort=OPPORTUNITY_SCORE`를 지정하면 기회 점수의 내림차순으로 정렬하며, 기회 점수가 없는 문서는 점수가 있는 문서 뒤에 배치한다. 점수가 같으면 감지 시각과 감지 ID의 내림차순으로 정렬한다. `sort`를 생략하거나 `LATEST`로 지정하면 기본 정렬을 사용한다. `runId`는 선택적인 모니터링 실행 식별자이며 지정하면 해당 실행에서 감지한 문서만 조회한다. `from`과 `to`는 선택적인 ISO 날짜·시간이며 감지 시각을 양 끝 포함 범위로 조회한다. 시작 일시가 종료 일시보다 늦으면 400 응답을 반환한다. 기관, 게시판, 제목, 변경 유형, 문서 버전의 첨부파일 수, 문서 자체 중요도, 회사 기회 점수·대응 우선순위와 마지막 확인 시각을 반환한다. 분석이 아직 없으면 분석 관련 값은 `NULL`이다. 대응 우선순위는 저장된 기회 점수 상세를 동일한 서버 규칙으로 계산한다. 목록 쿼리는 문서, 버전, 소스와 분석 결과를 한 번에 조회하여 건별 추가 조회를 만들지 않는다.

### 감지 문서 상세

```http
GET /api/document-detections/{detectionId}
```

프론트엔드가 문자열을 다시 파싱하지 않도록 다음 정보를 구조화하여 반환한다.

- 기관, 게시판, 제목, 게시일, 변경 유형과 원문 URL
- 파일별 이름, 확장자, 크기, 파싱 상태와 다운로드 URL. 첨부파일 수는 배열 길이로 계산한다.
- AI 요약, 핵심 내용 배열, 중요도와 근거
- 지원 가능성, 변경 유불리와 제안 방향

AI 분석이 아직 없으면 `analysis`는 `NULL`이며 문서 메타데이터와 첨부파일은 그대로 반환한다.

### 모니터링 실행 이력 목록

```http
GET /api/monitoring-runs?page=0&size=20
```

실행 요청 시각과 실행 ID의 내림차순으로 정렬한다. 실행 방식, 상태, 대상 소스 수, 감지 문서 수, 경고 수와 보고서 제목을 반환한다. 보고서가 없거나 아직 완료되지 않았으면 제목은 `NULL`이다. 실행과 보고서를 왼쪽 조인하여 보고서가 없는 실행도 목록에 포함한다.

두 API는 JWT 인증이 필요한 관리자 API다.
## 12. Frontend

제안 준비 체크리스트에는 충족·준비 완료·준비 필요·확인 필요 상태 뱃지를 표시하지 않는다. 회사가 실제로 보유한 증빙이나 사용자 확인 데이터 없이 준비 상태를 확정하지 않기 위해 제안 생성 모델, Python 결과 스키마, Spring 요청·응답 DTO와 프론트엔드 타입에서 체크리스트 `status` 필드를 제거한다. 기존 버전 10 결과는 버전 11로 한 번 재생성하며, 화면의 항목 수는 상태 필터 없이 실제 체크리스트 항목 수를 표시한다.

공고 분석 단계에서 신청 불가이거나 회사 적합도가 60점 이하인 제안 요청 공고는 `NOT_RECOMMENDED`로 코드 보정하고 제안 준비 데이터를 제거한다. 이는 후속 제안 생성의 61점 기준과 동일한 결정 규칙이며, LLM이 모순된 제안 상태를 반환해도 전체 공고 분석을 재호출하지 않게 한다.

Frontend는 React, TypeScript와 Vite로 구성하고 Spring Boot의 `/api` 요청을 로컬 개발 서버에서 `localhost:8080`으로 프록시한다. JWT는 브라우저 세션 동안만 `sessionStorage`에 보관하며 모든 관리자 API 요청에 Bearer 토큰으로 전달한다.

화면은 관리자 로그인, 모니터링, 감지된 게시글의 세 영역으로 구성한다. Telegram 설정 화면은 현재 범위에서 제외한다.

- 로그인은 서비스의 목적과 보안 안내를 친절하게 전달하고 관리자 인증에 집중한다. 왼쪽 소개 영역은 밝은 블루그레이 표면과 낮은 대비의 제품 미리보기로 구성한다. 오른쪽 로그인 영역은 깨끗한 흰색 배경을 사용하고, 흰색 카드의 은은한 경계와 그림자로 인증 폼의 시각적 중심을 유지한다.
- 모니터링은 소스 등록·수정·활성 변경, 수동 실행, 자동 실행 주기·요일·시각 설정과 실행 이력을 한 화면에서 관리한다. 화면이 보이는 동안 실행 이력을 5초마다 백그라운드 갱신하여 스케줄러가 만든 자동 실행과 진행 상태를 별도 새로고침 없이 표시한다.
- 감지된 게시글은 회사 대응 우선순위 필터, 기회 점수순 선택과 검색이 가능한 목록 및 문서 상세 패널로 구성한다. 목록은 기본적으로 최신순이며 `기회 점수 높은 순` 체크박스를 선택하면 전체 조회 결과를 기회 점수 내림차순으로 표시한다. 목록과 상세 화면은 회사 대응 우선순위와 기회 점수를 중심으로 표시한다. 문서 자체 중요도는 저장·보고서 정렬 등 내부 분석에 유지하되 사용자 화면에서는 별도 배지로 노출하지 않는다.
- 감지된 게시글은 최근 실행 결과를 기본 표시한다. 실행 선택기에는 `최근 실행 결과`와 `전체 실행 결과`만 제공하며 전체 실행 결과로 전환하면 이전 모니터링 이력을 모두 표시한다. 날짜·대응 우선순위·검색 필터는 선택한 실행 범위 안에서 적용한다.
- 상세 패널은 요약, 핵심 내용, AI 기회 점수, 지원 가능성, 변경 유불리, 제안 방향과 첨부파일을 별도 영역으로 표시한다. 첨부파일 처리가 실패하면 파일 크기 저장 여부를 기준으로 다운로드 실패와 내용 추출 실패를 구분하고, 파일 정보 옆에 간결한 상태 배지로 표시한다. 제안 방향은 아이콘과 제목만 한 행에 정렬하고 단계 카드는 컨테이너 전체 너비를 사용해 좌우 균형을 유지한다.
- AI는 게시글과 HWP·HWPX·PDF 및 ZIP 내부 지원 문서의 추출 텍스트를 함께 확인하여 일반 공지, 제출 양식 없는 사업 공고, 제안서 제출 공고와 추가 검토 대상으로 분류한다. 파일 확장자만으로 제안서 양식을 판정하지 않는다.
- 제안서 제출 공고에는 실제 양식 파일명과 작성 항목을 저장한다. 회사 적합도 61점 이상이고 신청 불가가 아니며 양식 항목이 확인된 경우에만 항목별 초안을 생성한다. 적합도 40점 이하 또는 신청 불가는 비권장, 그 사이는 추가 검토로 처리한다. 확인되지 않은 실적·인력·예산·인증은 회사 확인 필요로 표시하고 서명·날인·동의서는 생성하지 않는다.
- 상세 패널은 제안서 제출 공고로 판정된 경우에만 책갈피 형태의 `사업 제안` 탭을 표시한다. 이 탭은 공고 분석을 장문으로 반복하지 않고 제안서 작성을 위한 지원 준비 워크스페이스를 제공한다. 모든 공고에서 회의 안건, 지원 조건 체크리스트, 제출 서류 체크리스트와 제안 전략 한 장의 동일한 순서를 사용한다. 사용하지 않는 제안서 초안 생성 행동은 표시하지 않는다. 기존 분석 JSON에 준비안 필드가 없으면 레거시 핵심 초안을 표시한다.
- 지원 조건과 제출 자료는 필수·조건부·선택·내부 권장 수준, 신청·평가·선정 후·협약·수행·결과보고 시점, 적용 대상과 원문 근거를 구조화하여 저장한다. 공식 증빙이나 사용자 확인 없이 자격을 충족 확인으로 표시하지 않으며 공개정보만 있으면 충족 가능성 또는 확인 필요로 제한한다. 신청 단계 자료와 선정 이후 자료는 화면에서 구분하고 공고 사실과 AI 제안도 별도로 표시한다.
- 회사의 신청 접수기한이 분석일보다 지난 공고는 지원 가능성을 `INELIGIBLE`로 보정하고 사업 제안을 비권장으로 처리한다. 지원 준비 문장은 괄호식 메모와 `다음 행동` 접두어를 사용하지 않고 완전한 문장으로 표시한다. 제출 서류의 서식 번호는 단독 제목으로 표시하지 않으며, 필요한 경우 실제 파일명과 문서 안 위치를 함께 안내한다.
- AI 처리는 조건부 2단계로 수행한다. 첫 호출은 모든 문서의 공고 유형·회사 적합도·신청 가능성을 분석한다. 제안서 제출 공고이면서 회사 적합도 61점 이상이고 신청 불가가 아니며 파싱된 첨부 양식이 있는 문서만 후속 모델을 한 번 호출하여 근거 추출과 고정된 지원 준비 구조 생성을 함께 수행한다. 원본 전체 목차 복원과 완성 제안서 본문 생성은 모니터링 작업에서 제외한다. 일반 공고와 부적합 공고에는 추가 모델 호출을 하지 않는다. 후속 단계가 실패하면 기본 공고 분석은 저장하고 사업 제안만 추가 검토 상태로 남긴다.
- 사업 제안 준비안을 생성하지 않은 경우의 안내는 모델이 작성한 자유 문장을 사용하지 않는다. 일반 코드가 실제 후속 생성 조건인 회사 적합도 61점, 신청 가능 상태와 파싱된 첨부 양식 보유 여부를 평가하여 충족하지 못한 조건을 한국어 문장으로 조합한다. 신청 자격이 추가 검토 상태이면 공식 증빙 확인 필요성을 함께 안내하며 내부 enum 값은 사용자 화면에 노출하지 않는다. 모든 후속 생성 조건을 충족한 뒤 모델 호출 자체가 실패한 경우에는 실패 사유 안내를 유지한다.
- 회의 안건에는 참여 방식·인력·예산 등 내부 의사결정만, 지원 조건에는 자격과 결격 조건 및 회사 확인 행동을, 제출 서류에는 실제 파일과 작성에 필요한 내부 수치·실적·자료를 둔다. 별도 회사 입력 목록을 모델이 생성하지 않는다. 같은 확인 행동을 회의 안건에 복제하지 않으며 자격과 그 증빙은 각각 유지한다. 접수 마감일 자체는 지원 조건에서 제외하고 명백한 제출 문서는 일반 코드가 제출 서류 영역으로 이동하며 문서명·적용 단계·대상·원문 근거·현재 설명·행동이 같은 반복만 제거한다. 필수 제출 문서가 완료된 것으로 확인되지 않으면 준비 필요 상태로 보정한다.
- 모델의 제출 서류 생성 상한은 15개로 유지한다. 원문 대조 후 지원 조건의 최대 12개 항목이 제출 서류로 재분류될 수 있으므로 최종 Python 결과와 Spring 수신 스키마의 제출 서류 상한은 27개로 맞춘다. 재분류된 서류와 근거를 개수 제한 때문에 임의로 자르지 않는다. 최종 결과 검증도 후속 제안의 예외 처리 안에서 수행하며 실패한 게시글은 준비안을 비우고 REVIEW_REQUIRED로 전달한다. 검증 오류의 입력 원문을 로그·사용자 사유에 포함하지 않고 안전한 안내를 사용한다.
- 신청기업 명의의 승인·허가·인증과 기존 승인 품목의 사업화를 필수로 하는 공고는 파트너나 외주용역으로 직접 신청 자격을 대신할 수 있다고 제안하지 않는다. 규제특례 승인 제품 또는 서비스와 사업 개시 사실이 공식 증빙으로 확인되지 않으면 참여 판단을 보류하고 세부 과제명도 확정하지 않는다. 승인기업 대상 외주 수행은 해당 지원사업 신청의 대안 역할이 아니라 별도의 영업 기회로 구분한다.
- 제안 전략의 과제 제목은 판단 상태에 맞게 표시한다. 지원 권장과 조건부 지원 권장은 추천 세부 과제, 판단 보류는 과제 확정 조건, 지원 비권장은 재검토 조건을 사용한다.
- 회사 관점 공고 포인트는 상세 API의 `proposal.sections`를 사용한다. 현재 형식은 해석 문장과 고정 소제목별 핵심 문장·설명으로 표시한다. 과거 형식은 재해석하거나 새 소제목으로 바꾸지 않고 이전 분석 안내와 기존 번호 카드로 제공한다.
- 변경 영향은 현재 감지 건의 변경 유형을 우선한다. 신규 문서는 비교 대상 없음, 변경 없는 문서는 변경 없음으로 표시하고 수정 문서만 AI 유불리 판정을 표시한다.
- AI 요약의 문단 줄바꿈은 유지하며 분석이 없는 문서, 빈 목록, 로딩과 오류 상태를 각각 안내한다.

시각 언어는 밝은 중립 배경과 파란색 핵심 행동을 사용한다. 설명 문구와 충분한 여백으로 작업을 안내하면서, 목록과 상태 배지는 촘촘한 간격을 사용해 운영 화면의 정보 밀도를 유지한다. 데스크톱에서는 고정 사이드바와 상세 드로어를 사용하고 모바일에서는 하단 탐색과 전체 너비 상세 화면으로 전환한다.


지원 준비 스키마 12는 생성 모델의 companyInputs와 criticalGaps 출력을 제거한다. criticalGaps는 보고서 호환을 위해 기존 세 목록에서 코드로 선택해 저장한다. 상세 조회 API는 companyInputs를 반환하지 않는다. 과거 JSON은 조회 시 제출 파일을 제출 서류로, 원문에 근거한 필수·조건부 요건을 지원 조건으로, 나머지 내부 확인 정보를 회의 안건으로 옮긴다. 명확한 재분류가 어려운 과거 항목은 설명과 행동, 인용 출처를 함께 유지하며 저장된 원본은 수정하지 않는다. 스키마 11의 기존 제안 공고는 다음 모니터링에서 현행 정책에 따라 재분석된다. 이 변경 자체는 모델 호출을 추가하지 않으며 실제 속도·정확도 개선은 운영 사례로 별도 측정해야 한다.

### 공고 분석의 회사 관점 공고 포인트

- 사업 제안 탭과 후속 제안 생성은 유지한다. 공고 분석 영역 제목은 `우리 회사가 주목할 공고 포인트`, 별도 고정 설명 문구는 표시하지 않는다.
- 기존 `proposal.sections` 저장·API 구조를 유지한다. `우리 회사와 연결되는 부분`, `이 공고에서 중요하게 볼 점`의 정확히 두 항목을 생성한다. 화면에는 제목 바로 아래 두 개의 고정 카드만 표시한다. 상단 공고 해석은 생성하지 않으며 기존 저장 데이터의 해당 항목도 화면에서 제외한다. `현재 정보로 확인되지 않은 부분`은 생성·표시하지 않으며 기존 저장 분석의 해당 항목도 화면에서 제외한다. 중요한 미확인 조건은 새 분석의 `이 공고에서 중요하게 볼 점` 설명에 근거와 의미를 포함한다. 점수·문서 유형·변경 유형에 따라 제목을 바꾸지 않는다.
- 각 본문은 핵심 판단, 공고와 회사 정보의 구체적 근거, 의미와 한계를 충분히 설명한다. 260자·문장 수·접점 수 제한 없이 근거의 양과 복잡성에 맞추고 기존 스키마의 항목당 1000자 상한은 유지한다. 짧은 문단을 빈 줄로 구분하며 분량을 채우기 위한 반복과 추측을 금지한다. 코드에서 본문을 잘라내지 않는다. 기존 정중한 합니다체를 유지한다.
- 회사 정보와 실제 공고 요구의 연결 이유와 적용 범위를 구체적으로 설명한다. 정보 부족과 명확한 조건 불일치를 구분하고 기술 관련성을 자격 충족으로 확대하지 않는다. 회사 정보가 부족하면 분석의 한계를 명시한다. 별도 미확인 항목 제거는 전체 자격 충족을 의미하지 않는다.
- 활용 방안·참여 권고·파트너 섭외·실행 계획은 생성하지 않는다. 공고 특징과 확인 상태까지만 설명한다.
- 정규화는 제목으로 항목을 정렬하고 두 항목 외 추가 항목을 제외한다. 필수 항목 누락은 분석 불충분 안내로 대체하며 과거 전략 문단에 새 제목을 붙이지 않는다.
- 화면은 원래의 연한 파란색 외곽 배경·흰색 아이콘·번호가 붙은 흰색 단계 카드를 사용한다. 새 분석은 제목 바로 아래 고정 소제목·본문 카드를 표시하고 기존 분석도 원래 제목·본문을 동일한 번호 카드로 펼쳐 표시한다. 별도 강조 박스·접기 UI는 사용하지 않는다.
- 기존 저장 분석은 자동 재생성하거나 DB를 수정하지 않는다. 작은 이전 형식 안내와 기존 분석 카드를 표시하며 실제 다음 분석부터 새 형식을 제공한다. 분석 결과 재사용 정책은 변경하지 않는다.

- 공고 포인트 본문은 `근거:`·`핵심 판단:` 같은 라벨형 표제 없이 자연스러운 합니다체 문단으로 통일한다. 생성 지침의 예전 실행 전략 요구를 제거한다. 문장 종결 부호 누락과 알려진 라벨형 표제를 검증하여 기존 제한된 재시도 경로로 다시 작성하며, 코드로 미완성 문장을 잘라내거나 마침표를 붙이지 않는다. 종결 부호 검사는 의미적 완결성을 완전히 보장하지 않으며 실제 모델 출력 품질은 별도 확인한다. 기존 저장 본문은 자동 수정하지 않는다.

- 공고 분석의 별도 `이렇게 판단했어요` 영역과 전용 스타일을 제거한다. 판단 근거 데이터의 생성·저장·API는 기존 호환성을 유지하며 화면에 별도 박스나 대체 기능을 추가하지 않는다.

### 계정별 게시글 북마크

- 게시글 행 오른쪽의 북마크 버튼으로 저장·해제한다. 상세 열기 버튼과 별도 버튼으로 두어 클릭·키보드 동작이 겹치지 않으며 `aria-pressed`와 저장/해제 이름을 제공한다. 요청 중 중복 클릭을 막고 실패 시 상태를 변경하지 않는다.
- `저장한 게시글` 필터는 전체 실행에서 로그인 사용자가 저장한 문서만 조회한다. 전환 시 검색·우선순위·기간·페이지를 초기화하고 실행 선택을 숨긴다. 기존 점수 정렬·기간·검색·우선순위 필터를 사용할 수 있다. 검색·우선순위는 기존과 동일한 현재 페이지 필터다.
- 북마크는 내용 버전 ID를 기준으로 저장한다. 변경 없는 재수집은 동일 버전이므로 저장 상태를 공유하고 감지 시각·ID 내림차순으로 한 건만 표시한다. 수정본은 별도 버전이므로 자동 저장하지 않는다. 이전 버전과 수정본을 각각 저장하면 둘 다 저장 목록에 표시하며 해제도 독립적이다. 페이지 개수도 중복 제거 후 계산한다. 저장 목록 마지막 페이지에서 마지막 항목을 해제하면 이전 페이지로 이동한다.
- Oracle `DOCUMENT_BOOKMARKS`: `BOOKMARK_ID` PK, `USER_ID` → `APP_USERS`, `VERSION_ID` → `DOCUMENT_VERSIONS`, `(USER_ID, VERSION_ID)` UNIQUE, `DOCUMENT_BOOKMARKS_SEQUENCE` 사용. 계정 행 잠금으로 같은 계정의 저장·해제를 직렬화하여 반복 PUT을 멱등 처리한다.
- API: `GET /api/bookmarks/versions`는 내 버전 ID 배열, `PUT /api/bookmarks/versions/{versionId}`는 저장, `DELETE /api/bookmarks/versions/{versionId}`는 해제, `GET /api/bookmarks/documents?page=0&size=20&sort=LATEST`는 최신 감지 결과의 기존 페이지 응답을 반환한다. 목록은 선택적 `from`, `to`를 지원하며 실행 ID로 제한하지 않는다. 모든 API는 JWT 인증을 요구하고 계정 ID는 요청 값이 아닌 인증 정보에서 가져온다.
- 기존 DB에는 `docs/migrations/20260907_document_bookmarks.sql`을 한 번 적용한다. 데이터 보존을 위해 기존 DB에 `ddl-auto=create`로 재시작하지 않는다. 로컬 Oracle에 마이그레이션을 적용하고 로컬 비추적 설정의 `ddl-auto`를 `none`으로 변경하여 데이터를 보존한 상태로 백엔드를 재시작했다. 다른 환경에도 기존 데이터 보존 설정으로 적용한다.
- H2 Oracle 모드에서 실제 JPA 저장·계정 격리·최신 결과 중복 제거·기간 필터·해제를 검증한다. 테스트용 H2만 추가하며 운영 DB 의존성은 그대로 유지한다.

- 상세 화면 상단 원문 보기 옆에도 저장/저장됨 북마크 버튼을 제공한다. 목록과 동일한 상태·저장 함수를 공유하고 해제 후 저장 목록에서 항목이 빠져도 열린 상세의 버전 ID를 유지하여 다시 저장할 수 있다. 요청 중 중복 클릭을 막고 오류를 상세 화면에도 표시한다.

- 기존 문서 북마크에서 버전 북마크로 전환할 때 `docs/migrations/20260907_bookmark_versions.sql`을 서버 중지 상태에서 적용한다. 기존 저장 당시 버전 정보가 없으므로 전환 직전 저장 목록에 표시되던 최신 감지 버전에 연결한다. 기존 document_id 값은 감사용 nullable 열로 보존하고 새로운 행은 version_id만 사용한다. 구 API 경로는 제거하여 이전 화면이 문서 ID를 버전 ID로 잘못 저장하지 않게 한다.


### 법률 점검의 근거 범위와 판정 한계

- 후보 추출은 문단 내부 문장을 유지하고 줄바꿈·주석 뒤의 다만, 단, 예외, 이 경우, 그러나로 연결되는 단서를 함께 읽는다. 기존 대상·행위 키워드 탐색은 유지한다. 유형별 최대 6개, 후보당 최대 3,000자로 입력을 제한하며 후보 개수 초과와 개별 문단 잘림을 구분한다. 온전히 읽은 조항의 판정은 유지하되 후보 개수가 초과되면 검토 범위와 추가 조건·예외 확인 필요성을 명시한다. 잘린 조항의 확정과 불완전한 자료에서의 미발견 판정은 허용하지 않는다. 모든 표 구조와 참조 규정의 자동 복원을 보장하지 않는다.
- 인용은 공백 정규화 후 후보 전체와 일치해야 한다. 임의 문구를 덧붙이거나 예외를 삭제한 인용은 거부한다. AI 결과와 수신 DTO의 evidenceExcerpt 상한은 3,000자로 함께 변경한다. 불완전하게 잘린 후보는 인용으로 표시하지 않는다.
- 모델 응답 누락·오류·인용 검증 실패는 ASSESSMENT_INCOMPLETE다. 기존 NOT_FOUND와 CAUTION, RESTRICTION_FOUND는 유지하며 DATA_INSUFFICIENT를 추가한다. NOT_FOUND는 확보한 텍스트의 검색·판정 범위에서 미발견이라는 뜻이다.
- Spring Boot 분석 요청은 파싱 실패·미지원 첨부도 ID·파일명과 null 텍스트로 전달한다. 완료된 첨부만 추출 텍스트를 전달한다. AI는 빈 첨부 텍스트 수를 모든 판단의 설명에 추가하며 미발견은 자료 부족으로 변경한다. 발견한 제한과 미완료 판정은 유지한다. 현재 계약으로는 빈 텍스트의 구체적 실패 원인이나 미수집 외부 규정까지 확정하지 않는다.
- 상단 요약은 제한 조항 발견과 실제 신청 과제·비용·성과물의 중복 여부 미확정을 구분한다. 일반적인 공고 유사성만으로 두 사업의 법적 충돌을 확정하지 않는다. 자동 수집에서는 공고 쌍 해석을 호출하지 않는다. 사용자가 선택한 공고 쌍의 추가 해석은 아래 별도 API로 요청한다.
- 기존 저장 분석은 자동 재생성하지 않는다. 기존 제한 상태도 조회 시 조항 발견으로 표시하지만, 문맥 보존·자료 부족 판정은 새로 분석된 결과에 적용된다. 실제 모델 정확도·지연 및 브라우저 화면 검증은 별도 확인이 필요하다.


### 유사 공고 후보 선별의 정확도 보완

- 공통 핵심 주제와 코사인 0.78 이상을 함께 요구하던 기존 경로는 하이브리드의 한 선택 경로로 유지한다. 아래 의미 중심·어휘 중심 경로를 추가하며 제목만으로 의미 점수를 덮어쓰지 않는다.
- 핵심 주제 비교 전에 유니코드 표기·일부 한국어 조사·띄어쓰기와 명시적 동의 표현을 정규화한다. 예측 정비/예지보전, 불량 검출/결함 탐지, 스마트 팩토리/스마트 공장, 해외 공동연구/국제 공동연구를 연결한다. AI·연도·숫자·일반 안내 표현만으로는 공통 주제로 인정하지 않는다. 이 사전은 일반적인 의미 이해를 대체하지 않으며 등록되지 않은 바꿔쓰기는 놓칠 수 있다.
- 다른 비교 필드가 비어 있어도 저장된 사업 목적이 있으면 주제 비교에 사용한다. 목적이 없거나 JSON을 읽지 못하면 기존 원문·요약 경로를 사용한다.
- 같은 모델명·같은 차원의 숫자 유한 벡터만 비교하며 영벡터·손상 JSON·문자열 숫자·null은 제외한다. 코사인 계산은 큰 유한 값의 오버플로를 피하도록 스케일을 정규화한다.
- 하이브리드 순위로 정렬하고 동점은 반올림 전 코사인과 버전 ID 순으로 고정한다. API의 기존 정수 similarityScore는 코사인의 표시용 반올림 값이며 RRF 점수나 정확도가 아니다. 최대 3건과 최신 분석 버전 선별 정책은 유지한다.
- 상세 링크 detectionId는 비교에 사용한 documentVersionId의 최신 감지 이력을 조회한다. 더 새로운 미분석 버전으로 연결하지 않는다. 감지 이력이 없는 후보는 전체 조회를 실패시키지 않고 건너뛴 뒤 다음 후보로 채운다.
- 비교 화면은 의미 유사도와 핵심 주제를 충족한 후보라는 점을 설명하고, 결과 없음도 현재 분석된 데이터·비교 기준의 범위로 표현한다. 외부 모델 호출과 임베딩 입력은 변경하지 않으므로 기존 벡터를 그대로 사용한다.
- 회귀 테스트는 통제된 벡터로 선택·제외·정렬 정책을 검증한다. 실제 임베딩의 정답률·재현율을 측정한 결과가 아니다. 실제 공고 쌍에 정답을 붙인 평가와 임계값 조정은 후속 작업으로 남긴다.


### 추가 모델 호출 없는 하이브리드 유사 공고 검색

- `HybridNoticeRanker`는 같은 모델·차원의 유효 임베딩 후보에 대해 의미 점수 순 상위 20개(0.65 이상)와 어휘 점수 순 상위 20개(양수)를 각각 추출한다. 기관 동일 여부나 마감일을 필수 필터로 사용하지 않는다. 임베딩이 없거나 모델·차원이 달라 의미 비교를 할 수 없으면 강한 사업 목적 일치에 한해 어휘 전용 후보를 허용한다.
- 어휘 점수는 정규화한 제목·사업 목적의 단어 출현 여부를 사용한 BM25 방식이다. 제목 출현은 2, 목적 출현은 1로 계산하며 반복 횟수는 증폭하지 않는다. 현재 후보 집합에서 IDF와 평균 길이를 계산하고 k1=1.2, b=0.75로 길이를 보정한다. 본문 서식·법률 조항·제안서 문장은 입력하지 않는다.
- 두 후보 목록의 합집합을 RRF(k=60)로 정렬한다. 같은 검색 점수는 같은 순위를 부여하여 ID 순서 때문에 어휘 동점 후보가 유리해지지 않게 한다. 검색별 20개 창 밖의 후보는 해당 검색 순위 점수를 받지 않는다.
- 최종 선택은 ① 코사인 0.78 이상과 공통 핵심 주제, ② 어휘 후보이면서 코사인 0.65 이상·양쪽 중 핵심 단어가 적은 공고 기준 60% 이상 일치·목적 단어 2개 이상 일치, ③ 의미 후보이면서 코사인 0.90 이상·양쪽 목적 텍스트 존재·알려진 주제 충돌 없음 중 하나를 만족해야 한다. 후보 검색만 통과했다고 모두 표시하지 않는다.
- 의미 중심 경로에는 순환경제, 규제특례, 국제기술협력, 공연·창작, 제조, 전산 유지관리의 명시적 주제 표현이 양쪽에 있으나 겹치지 않는 경우를 제외하는 보수적 보조 규칙을 둔다. 이 제한된 규칙은 범용 의미 판별기가 아니며, 여러 분야를 연결하는 관련 공고를 놓치거나 사전에 없는 오탐을 허용할 수 있다. 모든 수치와 보조 규칙은 초기 정책이며 실제 데이터로 보정해야 한다.
- API `similarNotices[].matchBasis`에 HYBRID/LEXICAL/SEMANTIC을 추가한다. 기존 similarityScore는 실제 코사인 기반 값을 유지한다. 화면은 근거 중심 후보로 표시하며 통합 순위를 의미 정확도처럼 설명하지 않는다.
- DB는 현재 문서를 제외한 문서별 최신 분석만 조회하고 버전·문서·기관을 함께 로딩한다. 최신 문서 버전이 아직 미분석이면 최신 분석이 존재하는 버전을 선택한다. 현재 공고와 후보의 정규화된 제목·목적을 각각 한 번 처리하여 재사용한다. 기존 벡터·모델·DB 스키마를 유지하며 추가 외부 호출·벡터 재생성은 없다. 여전히 최신 분석 전체를 읽는 선형 탐색이므로 대규모 데이터의 지연은 별도 측정이 필요하다.
- 통제된 벡터 회귀 테스트에서 무공통단어 의미 후보, 0.70의 강한 목적·어휘 후보, 제목만 일치하는 후보 제외, 알려진 주제 충돌 제외, 검색 순위 결합, 의미 상위 20개 밖의 어휘 후보 복구를 검증한다. 실제 공고 정답률·재현율·조회 p95·비용 비교는 별도 평가 전까지 미측정이다.


### 법률 비교 화면의 반복 안내 정리

- 항목 오른쪽의 판정 미완료·자료 부족·제한 조항 발견·적용 여부 미확정 등 상태 문구를 숨기고 항목명과 접기 화살표만 표시한다. 상태값 생성·저장·API는 유지한다.
- 공고별 설명에 반복해서 붙는 `첨부 N개의 추출 텍스트가 없어 전체 자료 확인은 미완료입니다.` 문장만 화면 표시 시 제거한다. 기존 저장 결과도 재분석 없이 적용한다. 제한 미발견 설명과 의미 판정 미완료 설명 및 원문 인용은 그대로 표시하며, 원문 근거에는 이 문자열 처리를 적용하지 않는다.
- 상단 종합 설명은 표시하지 않고 하단 안내는 유지한다. 별도의 상태 배지나 대체 안내 영역을 추가하지 않는다.


### 법률 판정 미완료 원인 분리와 근거 선택

- 후보의 selection_limited는 다른 후보의 개수 제한 초과, incomplete는 해당 조항의 길이 제한에 따른 잘림을 뜻한다. 후보가 많다는 이유만으로 완전한 조항의 판정까지 버리지 않는다. 선택된 완전한 조항만 설명하며 개수 제한 시 다른 조항의 조건·예외는 추가 확인 대상으로 남긴다. 문단을 임의 분할해 예외를 잃거나 잘린 문단을 확정 근거로 쓰지 않는다.
- 법률 판정 내부 모델은 candidate_id로 원문 후보를 선택한다. 서버는 ID 범위·위험 유형·완전성을 검사한 뒤 해당 원문을 그대로 복원한다. 기존 evidence_excerpt 응답도 전체 일치 검증으로 호환한다. 외부 API·DB 스키마 변경과 추가 모델 호출은 없다.
- 자동 분석 호출 오류, 항목 결과 누락, 원문 근거 대조 실패, 검토 범위 제한, 모델이 판단한 추가 문맥 필요를 서로 다른 한국어 설명으로 반환한다. 기술적 실패를 원문 내용 자체가 모호하다는 뜻으로 설명하지 않는다. 내부 실패 원인별 설명은 ASSESSMENT_INCOMPLETE 상태를 유지한다.
- 단순 소유권 귀속 조항만으로 재사용 금지를 단정하지 않도록 모델 지침을 보완한다. 최종 의미 판정의 실제 모델 정확도는 별도 검증 대상이다.
- 기존 저장 결과는 수정하거나 자동 재분석하지 않는다. 새 근거 선택 및 원인별 문구는 새 분석에 적용된다. 기존 미완료 결과를 갱신하려면 해당 문서를 재분석해야 한다.


### 비교 방향 일관성과 법률 근거 미리보기

- 법률 원문 근거는 기존 비교 셀과 같은 6줄 미리보기를 사용한다. 실제 표시 영역을 넘을 때 전체 보기 버튼을 제공하고 기존 대화상자에서 원문 전체를 읽는다. 인용 데이터 자체는 줄이거나 요약하지 않는다.
- 어휘 중심 후보의 일치율은 공통 핵심 단어 수 / 두 공고 중 작은 핵심 단어 수로 계산한다. 통합 공고와 세부 공고의 포함 관계를 양쪽 방향에서 동일하게 판단한다. 의미 점수 0.65와 목적 단어 2개 공통 조건은 유지한다. 부분집합 관계의 허용 범위가 넓어지므로 실제 오탐률은 후속 평가한다.
- RRF 순위·검색별 20건·최종 3건 및 최신 분석 버전 선택 때문에 최종 목록 자체의 완전한 대칭은 보장하지 않는다. 과거 실행 결과를 그대로 보여주는 것이 아니라 조회 시 현재 분석 후보로 다시 계산한다.
- 문서 전환 때 이전 상세·비교 결과를 비우고, 취소된 조회의 뒤늦은 응답은 반영하지 않는다. 비교 조회 실패나 결과 미확인은 0건으로 표시하지 않고 별도 안내한다. 정상 응답의 빈 목록만 비교 0건으로 표시한다.

### 법률 비교 안내 표현 정리

- 사용자 요청에 따라 제목 아래 종합 설명을 제거한다. 항목 목록 위 간격은 유지한다.
- 검토 결과 누락·호출 오류·근거 대조 실패와 과거 의미 미확정 설명을 화면에서 이해하기 쉬운 안내로 바꾼다. 결과 누락은 `검토가 완료되지 않았습니다. 원문을 확인해 주세요.`로 표시하며 완료된 판단으로 표현하지 않는다. 저장된 결과에도 재분석 없이 적용하고 원문 인용과 내부 상태는 변경하지 않는다.

### 법률 조항 해석과 누락 응답 보완

- 단순 상태 분류에 더해 interpretation(140자), implication(140자), verification(100자)을 내부 모델 응답에 추가한다. 원문의 적용 대상·조건·예외를 유지하며 신청·수행에 미치는 조건부 영향과 대조해야 할 사실을 간결하게 설명한다. 새로운 법령·제재·의무를 만들거나 실제 두 사업 충돌을 단정하지 않도록 지시한다.
- 근거 ID·유형·원문 완전성 검증을 통과한 제한/주의 결과만 해석을 기존 summary에 저장한다. 코드 검증은 인용 연결을 확인하며 해석의 법률적 정확성을 보증하지 않는다. 외부 API와 DB 스키마는 유지한다.
- 최초 응답의 누락·근거 오류·해석 누락·미완료 유형은 동일 후보 ID를 유지하여 최대 한 번 보완한다. 전체 두 호출은 공통 최대 60초 예산 내에서 실행한다. 보완 오류·시간 초과 시 앞서 완료된 유형은 유지한다. 정상 결과는 추가 호출하지 않는다.
- 미완료 후보를 확정 근거로 오해하지 않도록 비교 화면 인용 제목은 관련 원문으로 표시한다. 검토 실패는 성공으로 바꾸지 않는다. 기존 저장 결과에 새 해석을 추가하려면 재분석이 필요하다.
- 실제 모델 응답 품질·공고 쌍별 적용 정확도·응답 지연은 실데이터 후속 검증 대상이다. 이번 변경은 공고별 조항 해석이며 두 공고를 한 번에 입력하는 별도 비교 모델은 추가하지 않는다.

### 법률 미완료 결과 재처리와 필수 응답

- 모델 전용 응답을 다섯 위험 유형의 필수 키로 고정한다. 상태는 enum이며 후보 없는 유형은 NOT_FOUND로 반환한다. 빈 리스트가 정상 응답으로 통과하는 경로를 제거한다. 내부 모델 응답만 바꾸며 저장/API 형식은 유지한다.
- 변경 없는 문서도 법률 결과가 없거나 유형 누락·중복·잘못된 상태·빈 요약·ASSESSMENT_INCOMPLETE이면 다음 수집 실행에서 분석 대상으로 포함한다. DATA_INSUFFICIENT만으로 반복 요청하지 않는다. 기존 결과는 새 응답 수신까지 유지한다.
- AI 캐시는 ASSESSMENT_INCOMPLETE 결과를 저장하지 않는다. 실행 내부의 일반 분석 재시도에서도 실패 결과의 고착을 방지한다. 외부 모델 장애나 원문 부족은 성공으로 위장하지 않는다.


### 법률 판정 신뢰성·선택 재처리 (검토 버전 2)

- 모델 출력과 내부 판정의 해석 길이 상한을 140/140/100자로 일치시킨다. 구조화 파서가 한 항목을 거부해도 원시 구조화 응답에서 유효한 다른 항목을 개별 복구한다. 원시 응답은 저장·로그에 남기지 않는다. 법률 해석을 임의로 잘라 유효한 답으로 만들지 않는다.
- 불완전한 줄바꿈은 이어 읽고, 명시적 불릿·빈 줄·완료 문장은 경계로 유지한다. 이어지는 단서와 그 다음 줄은 같은 조항으로 보존한다. 대상·행위 키워드는 첫 출현만 비교하지 않고 가까운 모든 출현을 선형으로 탐색한다. 연구비·연구수당 및 이중으로 지급 표현을 후보에 포함한다. 원문 분할은 소스별 한 번 수행한다.
- 유형별 최대 6개·조항별 최대 3,000자는 유지하며 완전한 조항을 먼저 선택한다. 다섯 유형을 번갈아 선택해 총 후보 원문을 18,000자로 제한한다. 예외를 잘라 공간을 맞추지 않으며 누락된 후보가 있으면 selection_limited를 유지한다.
- 법률 호출은 총 60초 안에서 최대 2회다. 최초 시도는 예산의 70%까지 사용하고 보완 시간을 남긴다. 보완 입력에는 미완료 유형의 후보만 원래 ID로 포함한다. 원문/참조 규정 필요(CONTEXT_REQUIRED)와 조항 잘림/검토 범위 제한(CONTEXT_LIMIT)은 같은 입력으로 즉시 재호출하지 않는다.
- 각 법률 결과에 선택적 failureReason을 추가한다. 원문에 없는 취소·배제·환수·처벌 등 제재 표현은 해석에서 거부하고 해당 항목만 보완한다. 보완이 실패하면 검증된 조항 발견 결과와 안전한 기본 설명을 보존한다. 이 어휘 검증은 법률적 의미의 정확성을 보증하는 검증이 아니며 실제 공고 평가가 필요하다.
- 변경 없는 문서는 전체 분석과 법률 전용 분석을 구분한다. 요청의 analysisScope는 FULL/LEGAL_ONLY(기본 FULL), legalReviewTypes는 선택적 유형 배열이다. 제안 스키마 갱신이 필요한 문서는 FULL을 유지한다. 법률만 필요한 문서는 계획·공고 분석·임베딩·사업 제안을 호출하지 않는다.
- 기존 내부 분석 결과 API에 legalResults 배열을 추가한다(기본 빈 배열). 각 항목은 detectionId/documentId/versionId/legalRisks이며 기존 results/failures와 같은 실행·문서 관계를 검증한다. 법률 결과만 기존 comparison_summary의 해당 유형에 병합하며 요약·사업 제안·기회 점수·벡터는 보존한다. 결과 전달 jobId가 같으면 재수신으로 재시도 횟수를 늘리지 않는다. 법률 전용 실행도 기존 제안 완료 알림 경로로 보고서 생성을 이어간다.
- comparison_summary JSON에 legalReviewVersion=2, legalReviewAttempts, legalReviewedAt(UTC), legalReviewJobId를 저장한다. 별도 DB 마이그레이션은 없다. 기존 버전 결과는 다음 수집에서 법률만 한 번 갱신한다. 같은 버전의 기술적 실패는 30분 이후에만 재처리하며 최초 포함 최대 3번이다. 자료 부족·추가 문맥 필요는 같은 원문으로 반복하지 않는다. 상한 이후 자동 호출은 멈추며 자료·검토 버전 변경을 통한 별도 갱신이 필요하다.
- 양쪽 서비스의 요청/응답 계약이 함께 바뀌므로 Python·Spring Boot를 함께 갱신해야 한다. 기존 DB의 결과를 즉시 삭제하거나 전체 재분석하지 않는다.

### 유사도 조회의 재사용과 의미 점수 없는 후보

- 검색은 최대 5,000개 분석의 정규화 단어·단위 벡터만 메모리에 보관한다. JPA 엔티티나 공고·법률 원문을 캐시에 보관하지 않는다. 매 조회의 분석 개수·최대 ID·최근 수정시각과 최대 60초 수명을 기준으로 갱신한다. 상한을 넘으면 조회 시 계산하며 후보를 임의로 잘라 누락시키지 않는다. 초기/변경 조회에는 전체 최신 분석 로딩, 모든 조회에는 후보별 내적·어휘 순위 계산이 남는다.
- 비교 가능한 벡터가 없는 경우, 어휘 검색 상위 20개 중 전체 핵심 단어 포함률 80% 이상·목적 단어 포함률 80% 이상·목적 핵심 단어 3개 이상 공통·알려진 주제 충돌 없음에 한해 LEXICAL_ONLY로 표시한다. 포함률 분모는 양쪽의 작은 집합이다. 유효한 의미 점수가 낮은 후보가 이 경로로 임계값을 우회할 수는 없다.
- LEXICAL_ONLY의 similarityScore는 null이며 화면은 사업 목적·어휘 일치 후보로 표시한다. 기존 HYBRID/LEXICAL/SEMANTIC과 최대 3건·정확한 분석 버전의 상세 링크 정책은 유지한다. 수치 기준은 초기 정책이며 실제 정답 라벨에 대한 정밀도·재현율 측정은 별도다.

### 선택한 두 공고의 조건부 해석

- 비교 탭의 주의사항 요약 버튼이 POST /api/document-detections/{currentId}/similar-notices/{similarId}/legal-review를 호출한다. 인증된 사용자가 현재 비교 후보 중 선택한 공고만 요청할 수 있다. 서버는 조회 트랜잭션을 끝낸 뒤 Python POST /internal/monitoring/legal-pair-review를 호출한다.
- 공고별 RESTRICTION_FOUND/CAUTION이고 원문 인용이 있는 경우만 verifiedEvidence(side/interpretation/excerpt)에 포함한다. 미완료의 관련 원문은 추가 해석 근거에서 제외한다. 모델에는 양쪽 사업 목적·자격·파트너, 검증된 원문과 회사 프로필을 제공한다. 회사 정보는 적용 맥락이며 법률 원문 근거가 아니다. 이전 생성 해석을 원문 대신 사용하지 않는다.
- comparison/implication/verification과 근거 ID를 생성한다. 해당 유형의 제공된 원문 ID 연결과 미지원 제재 표현을 검증한다. 실제 과제·예산·권리 정보가 없으므로 위반·동시 신청 가능을 확정하지 않으며, 한쪽 근거 부재를 그 공고의 제한 부재로 해석하지 않도록 지시한다. 인용 연결 검증만으로 모든 해석 오류를 제거할 수는 없다.
- 위험 유형별 양쪽 원문을 함께 유지하면서 총 원문 18,000자 이내를 선택한다. 모델은 제공된 근거의 유형만 해석한다. 전체 대기·생성 30초 안에서 최초 최대 20초와 필요 시 1회 보완을 허용한다. Spring Boot 읽기 제한은 35초다. 추가 해석이 실패해도 기존 비교표·공고별 법률 결과는 유지한다.
- Python 프로세스별 최대 동시 2건·진행/대기 8건·결과 128건의 제한을 둔다. 같은 입력 요청은 진행 중 호출을 공유하며 성공은 1시간, 실패는 연속 요청 방지를 위해 30초 재사용한다. 목적·조건·원문·회사 프로필·데모 사용 여부·해석 지시 변경은 다른 캐시 키가 된다. 영구 저장은 하지 않으며 서버 재시작 후 다시 생성한다. 기본 수집 시에는 이 호출이 발생하지 않는다.
- 문서·비교 대상 또는 법률 결과가 바뀌면 화면의 추가 해석을 초기화하고 이전 요청 응답을 무시한다. 모델 실패를 정상 검토로 표시하지 않는다.

### 공고 비교 주의사항 화면 표현

- 제목은 `함께 신청할 때 주의할 점`, 실행 버튼은 `주의사항 요약`으로 표시한다. 두 공고의 중복지원 제한, 비용 중복 청구, 제안서 재사용 조건을 비교하는 기능임을 한 문장으로 설명한다.
- 기존 법률 영역의 따뜻한 배경 안에서 제목·설명·버튼을 한 줄로 배치하고 별도의 큰 안내 카드를 두지 않는다. 본문은 12px, 제목은 14px로 구분하며 좁은 화면에서는 버튼과 결과의 항목·설명을 세로 배치한다.
- 생성 결과는 `공고별 조건`, `주의할 점`, `직접 확인할 내용`으로 나눈다. 원문은 기존 공고별 접이식 항목에서 확인한다.
- 자료 부족은 비교에 필요한 조항이 부족하다고 안내하고 기존 공고별 내용·원문으로 안내한다. 사용자가 수행할 수 없는 검토 완료를 요구하지 않으며 같은 자료로 반복하는 요약 버튼은 숨긴다. 일시 실패에는 `다시 시도`를 제공한다. 서버의 과거 안내 문구도 상태에 따라 같은 사용자 문구로 표시한다.
- 모바일에서 상세 창의 데스크톱 폭·좌우 여백이 모바일 규칙을 덮어쓰는 문제를 수정한다. 760px 이하에서는 상세 창을 전체 폭으로 표시하고 내용 좌우 여백 16px, 법률 영역 바깥 좌우 여백 0으로 중첩 여백을 줄인다.

### 공공기관 모니터링을 위한 회사 데모 프로필

- 목적은 모니터링한 공공기관의 실제 공고에서 회사에 유용한 인사이트를 도출하는 것이다. 회사 데이터 파일을 나누지 않고 기존 `BISTELLIGENCE_PROFILE.demo_profile`에 가상 업무·제공 역량·자원 수요·참여 여력·준비 상태를 보관한다. 기존 실제 회사 소개·공개 사실·미확인 항목을 가상 정보로 덮어쓰지 않는다.
- 페르소나는 반도체·디스플레이·철강 고객의 제조 데이터·AI 소프트웨어 업무를 수행하며 공공 발주, 공공기관의 기업 매칭·실증, 필요한 자원 지원을 찾는 기업이다. 고객 A/B/C는 익명 가상 사례이며 실제 고객사와 연결하지 않는다. 고객 유상 계약에서 정부지원 이력을 추론하지 않는다.
- 이전 데모의 특정 신규 정부과제·선정 이력·3억 원 사업비·연간 참여율·고정 신청 일정은 제거한다. `monitoring_purpose`, `operating_context`, `customer_projects`, `supply_capabilities`, `resource_needs`, `staff_capacity`, `participation_constraints`, `documents`, `reusable_assets`, `support_history`, `notice_evaluation_criteria`, `open_questions`로 현재 상황을 정리한다.
- 회사의 공급 역량은 데이터 연계·모델 검토·분석 화면·시스템 구축이다. 회사의 수요는 추가 GPU 이용과 독립 시험·평가다. GPU 구매·임차 입찰의 공급업체 모집과 GPU 이용 지원의 수혜기업 모집을 구분하며 제조 소프트웨어 기업을 제조공장 보유 기업이나 GPU 판매업체로 간주하지 않는다.
- GPU 데모는 두 개발팀이 24GB급 1대를 공유해 실험 시작이 지연된 가정이다. 2026-10-01~2027-01-31의 독립 작업 슬롯 2개·슬롯당 48GB급은 계측 전의 희망 사양이고 전체 4개월 자부담 상한 800만 원도 내부 초안이다. 기존 예약 조정의 대안·제공 사양·사용 기간·전체 비용·데이터 권한·보안 조건으로 필요성을 판단한다. 고객 데이터의 외부 사용은 미승인이고 공개·합성·권리 확인 자료가 첫 이용 후보이다.
- 별도 시험 지원 수요는 시계열 예측·영상 분류의 평가 방법과 시험환경 확보다. 비용 한도 미정은 null로 유지하여 무료 또는 비용 없음으로 해석하지 않는다. 자원 지원 검토용 현금 2천만 원은 가상 내부 한도이며 GPU 800만 원을 포함한다. 신규 공공 사업의 수행 원가·자부담은 공고별 검토 전이다.
- 5명 팀의 현재 업무 배정·추가 참여 여력·예비 여력 합계는 역할별 100%다. 추가 여력은 총 80%, 상근 환산 0.8명으로 소규모 검토·시험 기준이다. 대규모 구축이나 상주 요구는 재배치 승인 없이 가능으로 표시하지 않는다. 인건비 계상·신청 자격 판정과 내부 인력계획을 구분한다.
- 서류 10종은 AVAILABLE(가상 보유), MISSING(가상 미보유), UNKNOWN(미확인), 담당 역할, 내부 확인 목표일·선택적 유효기간을 가진다. 증빙 보유와 공고 요건 충족은 다르다. 내부 목표일을 실제 공고 마감으로 쓰지 않는다. 보도자료·선정 결과·시설 소개로부터 없는 모집·혜택·신청 절차를 만들지 않는다.
- 인사이트의 연결 기준은 공고 원문 근거 → 현재 업무/자원 수요 → 기대 실익과 제약 → 확인할 조건 → 원문에 있는 신청 방법·기한·서류를 기준으로 한 다음 행동이다. 신규 고객 기회는 공공기관이 공개한 발주·상담·매칭·실증 경로에 근거하며 일반 기업의 미확인 구매 의사·고객명·연락처를 생성하지 않는다. 회사 수요에 맞지 않는 공고는 낮은 우선순위 또는 제외 사유를 설명한다.
- 사용자 요청에 따라 `company_profile.py`의 `USE_DEMO_COMPANY_PROFILE=True`를 실행 기본값으로 둔다. 일반 공고 분석과 사업 제안의 `AnalysisToolContext`는 실제 회사 소개와 `demoProfile`을 함께 읽고, 유사 공고의 추가 해석도 동일 직렬화 함수를 사용한다. 파일은 나누지 않는다. `include_demo_profile=False` 또는 `read_company_profile(..., include_demo=False)`로 해당 컨텍스트에서 제외할 수 있다. 공통 기본값을 바꾸면 Python 프로세스를 다시 시작해야 한다.
- 데모는 SYNTHETIC_DEMO·isVerifiedCompanyFact=false·사용 정책과 함께 별도 하위 객체로 전달한다. 실제 verifiedFacts와 unknownFields를 보존한다. 사용자 요청에 따라 분석 본문에서는 데모를 회사의 현재 운영 상황으로 적용하고 우리 회사의 자원·업무·서류 상태로 자연스럽게 서술한다. 데모 여부는 별도 화면 안내와 메타데이터로 표시한다. 명시된 자원·서류 보유 상태는 운영 판단에 사용하되 원본 증빙 검증·공고별 유효성·지원 자격 충족을 자동 확정하지 않는다. 공통 프롬프트에 GPU 공급업체/수혜기업 구분, 실제 공고 날짜 대조, 미확인과 미보유 구분을 지시한다. 모델이 이 의미적 구분을 언제나 준수함을 보장하는 검증은 아니다.
- Python은 실제 입력에 데모가 포함됐는지로 `proposal.usesDemoProfile`을 설정한다. 후속 제안 생성·생성 실패·생략 경로에서도 사용 표시를 보존하고 Spring Boot의 저장 JSON·상세 응답을 거쳐 화면 안내에 사용한다. 공고 쌍 해석의 완료 응답에도 `usesDemoProfile`을 포함한다. 과거 응답에서 이 필드가 없거나 null이면 백엔드는 false로 처리한다. 새 표시는 전체 결과가 가상이라는 뜻이 아니라 일부 회사 맥락에 데모가 포함됐다는 뜻이다.
- 유사 공고 후보의 검색·유사도 산식과 원문 법률 조항 추출은 기존 공고 기준을 유지한다. 회사 프로필은 선택한 두 공고의 추가 해석에 사용한다. 프로필을 포함한 전체 프롬프트의 해시를 캐시 키로 사용해 프로필 변경 전 해석이 재사용되지 않도록 한다. 동일 입력의 동시 호출 공유·시간/호출 상한은 유지한다. 별도 모델 호출을 추가하지 않지만 입력량 증가에 따른 지연·토큰 비용은 실모델 측정 대상이다.
- 새 설정은 새로 생성하는 분석·해석부터 반영한다. 기존 저장 결과를 일괄 재생성하거나 데모 사용 결과로 소급 표시하지 않는다. 모의 모델 테스트로 입력 전달·저장/조회 호환성·캐시 갱신을 검증하며, 실제 공고에 대한 생성 품질 평가는 별도로 수행한다.


### 공고 포인트의 현재 시점과 참여 역할 검증

- 일반 분석·사업 제안·두 공고 추가 해석에 동일한 참여 역할 지시를 제공한다. 수요기업의 자격·서류를 공급기업의 결격으로 옮기지 않고, 비영리 주관기관 조건을 회사의 공급기업/용역 참여 가능성과 구분한다. 컨소시엄 전체 부담 비율을 회사 단독 부담으로 해석하지 않도록 한다. 이는 의미 해석 지시이며 모든 역할 오판을 코드로 검증한 것은 아니다.
- 날짜가 명확한 비교 요약의 접수 마감일을 시급성 설명보다 우선해 분석일과 대조한다. 완전한 날짜 두 개로 구성된 접수 기간은 종료일을 사용한다. 모호한 복수 날짜·연도 없는 종료일은 이 추가 경로에서 추정하지 않는다. 같은 날의 시각 단위 마감 판정은 이번 보정 범위가 아니다.
- 접수 종료는 신청 불가·시급성 0·제안 생성 비권장으로 보정하고 해당 사유를 보존한다. 공고 포인트의 두 번째 항목에도 종료 안내를 추가한다. 1000자 상한을 넘으면 내용을 임의로 자르지 않고 기존 제한된 재시도에서 다시 작성하도록 한다.
- 마감된 공고의 요약·핵심 사항·공고 포인트에서 현재 신청·제출·발표 준비 권고와 지난 평가를 예정으로 설명하는 표현을 검사한다. 정상적인 과거 서술·명시된 차년도/후속 공고 조건부 설명·불가 안내는 보존한다. 모순은 기존 최대 재시도 안에서 수정하며 한도를 소진하면 잘못된 분석을 정상 결과로 저장하지 않는다. 한국어 표현 기반 검사로 모든 의미적 모순을 탐지함을 보장하지 않는다.
- 일반 분석·제안 설명·비교 해석에서 생성된 회사 서술의 '데모 가정에서는' 등 접두어를 정리한다. 공고 비교의 원문 필드와 법률 인용·서류 출처는 정리 대상에서 제외한다. 내부 SYNTHETIC_DEMO 표시는 유지한다. 제안 및 두 공고 해석에도 한국 시간 기준 분석일을 전달하며 두 공고 해석 캐시는 날짜가 바뀌면 갱신된다.


## 첨부 양식 선택형 제안서 본문 작성

사업 제안 탭 아래의 **제안서 초안 작성**에서 사용자가 첨부파일을 직접 선택하고 초안 생성을 요청한다. 기존의 자동 제안 전략·준비 체크리스트와 별도 기능이며 자동 모니터링마다 모델을 추가 호출하지 않는다. 기존 점수·생성 여부와 관계없이 본문을 읽은 첨부를 선택할 수 있다. 접수가 종료된 공고에서는 향후 제안용 참고 자료임을 화면에 표시한다.

- 원본 파일은 기존과 같이 임시 다운로드 후 삭제한다. 이 기능은 Oracle의 첨부 추출 본문을 읽으며 원본 파일을 다시 다운로드하거나 편집하지 않는다.
- 첨부는 감지 결과의 정확한 문서 버전을 기준으로 조회한다. 다른 버전의 첨부 ID나 없는 내부 문서를 요청하면 404, 파싱 실패·빈 본문·길이 초과 파일은 400으로 거절한다.
- ZIP은 저장된 `[파일: 내부 경로]` 구분자를 기준으로 내부 문서를 개별 선택한다. 선택한 내부 문서만 양식 입력으로 전달한다.
- 첫 모델 호출은 실제 서술형 작성 항목 중 핵심 항목 최대 4개를 선택한다. 모델은 제목을 다시 쓰지 않고 제목이 적힌 연속된 원문 줄 번호 최대 3개만 선택한다. 코드는 해당 줄에서 제목을 가져오고 다음 선택 항목 이전의 인접 원문 최대 8줄에서 연속 인용을 직접 가져와 전체 선택 양식과 공백 정규화 후 대조하고 원문 순서로 정렬한다. 원문의 오타와 여러 줄에 걸친 제목도 보존한다. 단순 신청인 정보, 동의서와 일반 공고문에는 임의의 목차를 만들지 않는다. 작성 항목이 4개 미만이면 확인된 항목만 작성하고 개수를 안내한다.
- 두 번째 호출은 회사 실제 소개와 현재 설정의 데모 운영 정보를 사용해 항목당 400~2,600자의 본문을 한 번에 작성한다. 당사 관점의 합니다체, 일관된 제안 범위, 현재 사실과 향후 수행 계획 구분, 근거 없는 실적·수치·자격 생성 방지를 지시한다. 제목은 검증된 첫 단계 결과로 고정하며 모델이 다시 작성하지 않는다.
- 항목 누락·중복, 존재하지 않는 회사 근거 ID, 불완전한 종결과 작성 안내 표현을 검증하고 스키마 파싱 오류를 포함해 최대 1회 본문 재작성을 허용한다. 본문의 내부 근거 괄호와 데모 수식어는 별도 출처 안내를 유지한 채 정리하며, 시험 시나리오·데모 영상 같은 기술 표현은 보존한다. 인용의 존재와 문체를 코드로 검증하더라도 회사 근거의 의미 연결이나 모든 사실의 정확성을 보장하는 것은 아니다. 최종 제출 전 사용자 검토가 필요하다.
- 화면에는 본문과 분리된 확인 사항, 펼쳐 볼 수 있는 원문 인용·선정 이유, 실제 입력에 따른 `usesDemoProfile` 안내를 표시한다. 항목별 및 전체 복사는 제목·본문만 복사하고 근거·확인 목록은 섞지 않는다. 탭 전환과 첨부 재선택 시 같은 상세 화면의 완료 결과를 유지한다.
- 선택 양식은 최대 80,000자로 제한하고 초과 시 임의로 자르지 않는다. 게시글과 선택 양식 외의 공고·RFP·제안요청·안내 첨부 최대 3개를 참고 공고 본문으로 구분해 전달하며, 앞뒤를 보존해 합계 최대 16,000자로 제한한다. 다른 양식의 제목을 작성 목차로 사용하지 않는다. `PROPOSAL_MODEL` 설정을 재사용하고 모델 재시도는 SDK에서 끈다. 요청 전체 시간은 대기·최대 3회 모델 호출을 포함해 `min(PROPOSAL_TIMEOUT_SECONDS, 180초)`, 동시 모델 실행은 2개, 대기 포함 진행 요청은 8개로 제한한다. Spring 읽기 제한은 190초다.
- 동일 문서 내용·회사 정보·데모 모드·한국 날짜·모델의 진행 요청은 공유하고, 완료 결과만 Python 메모리에 1시간·최대 64건 보관한다. 실패 결과는 보관하지 않아 즉시 재시도할 수 있다. 새 DB 테이블이나 영구 초안 저장은 추가하지 않으며 화면에 복사 보관 안내를 제공한다.

### API 계약

| 경로 | 요청 | 응답 |
|---|---|---|
| `GET /api/document-detections/{detectionId}/proposal-sources` | JWT 인증, 감지 ID | `attachmentId, partIndex, fileName, attachmentName, available, reason` 목록 |
| `POST /api/document-detections/{detectionId}/proposal-draft` | JWT 인증, `{attachmentId, partIndex}` | 공통 성공 응답 안에 아래 초안 결과 |
| `POST /internal/monitoring/proposal-write` | `title, noticeText, fileName, templateText` | Python 초안 결과 |

초안 결과는 `status=COMPLETED|NEEDS_TEMPLATE|UNAVAILABLE`, `fileName`, `usesDemoProfile`, `message`, `sections`로 구성한다. 각 항목은 `title, body, sourceQuote, selectionReason, companyEvidence[], confirmationItems[]`를 반환한다. 양식 확인 실패와 일시 생성 실패는 빈 항목 및 안전한 안내를 반환한다. Spring의 DB 읽기 트랜잭션은 모델 요청 전에 종료한다.
