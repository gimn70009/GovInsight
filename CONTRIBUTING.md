# GovInsight 개발 규칙

## 커밋 메시지 형식

커밋 메시지는 다음 형식으로 작성한다.

```text
type: 한글 설명
```

예시:

```text
feat: 모니터링 소스 등록 기능 추가
fix: 게시글 중복 저장 문제 수정
docs: 모니터링 처리 흐름 문서 작성
```

## 커밋 타입

| 타입 | 사용 시점 |
|---|---|
| `feat` | 새로운 기능을 추가할 때 |
| `fix` | 버그를 수정할 때 |
| `docs` | 문서만 추가하거나 수정할 때 |
| `refactor` | 기능 변화 없이 코드 구조를 개선할 때 |
| `test` | 테스트를 추가하거나 수정할 때 |
| `chore` | 프로젝트 구조, 설정 등 기타 작업을 할 때 |
| `build` | Gradle, 의존성, 빌드 설정을 변경할 때 |
| `ci` | GitHub Actions 등 자동화 설정을 변경할 때 |
| `style` | 코드 동작에 영향 없는 형식이나 스타일을 수정할 때 |

## 작성 규칙

- 타입은 영어 소문자로 작성한다.
- 타입 뒤에 콜론과 공백을 넣는다.
- 설명은 한글로 간결하게 작성한다.
- 설명 끝에는 마침표를 붙이지 않는다.
- 한 커밋에는 하나의 목적만 담는다.
- `수정`, `작업`, `변경`처럼 내용이 불분명한 설명만 단독으로 사용하지 않는다.

## 프로젝트 예시

```text
chore: 모노레포 초기 구조 구성
docs: 영역별 개발 지침과 모니터링 아키텍처 작성
feat: 모니터링 소스 등록 API 구현
feat: Python 비동기 수집 작업 접수 기능 구현
fix: 자동 스케줄 중복 실행 문제 수정
refactor: 텔레그램 전송 로직을 별도 서비스로 분리
test: 게시글 변경 감지 단위 테스트 추가
build: Oracle JDBC 의존성 추가
```
